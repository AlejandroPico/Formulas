export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','bio-monod-sim');root.style.setProperty('--equation-color','#0284c7');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>sustrato [S]</span><input id="s" type="range" min="0" max="100" step="2" value="10"><output>10 mg/L</output></label><label><span>K_s afinidad</span><input id="ks" type="range" min="5" max="45" step="1" value="15"><output>15</output></label>';
const s=controls.querySelector('#s'),ks=controls.querySelector('#ks'),outs=controls.querySelectorAll('output');const mumax=1.2;
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const S=+s.value,K=+ks.value,mu=mumax*S/(K+S);outs[0].textContent=S+' mg/L';outs[1].textContent=K.toFixed(0);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Modelo de Monod: crecimiento microbiano',24,36);ctx.font='14px monospace';ctx.fillText('μ = μmax · S/(Ks+S)',24,64);const x=62,y=H-82,w=W*.58,h=H*.56;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x,y-h);ctx.lineTo(x,y);ctx.lineTo(x+w,y);ctx.stroke();ctx.strokeStyle='#0284c7';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const sv=i/260*100,m=mumax*sv/(K+sv),yy=y-m/mumax*h;if(i)ctx.lineTo(x+i/260*w,yy);else ctx.moveTo(x,yy)}ctx.stroke();const px=x+S/100*w,py=y-mu/mumax*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,6,0,7);ctx.fill();ctx.strokeStyle='#f59e0b';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(x+K/100*w,y);ctx.lineTo(x+K/100*w,y-h*.5);ctx.stroke();ctx.setLineDash([]);badge('Ks: μ=μmax/2',x+K/100*w-35,y-h*.53,'#fde68a');badge('μ='+mu.toFixed(3),px+10,py-8,'#bae6fd');readout.innerHTML='<b>μ='+mu.toFixed(3)+' h⁻¹ · μmax=1.20 h⁻¹</b><br>A bajo sustrato la cinética es casi lineal; a alto sustrato aparece saturación metabólica.'}
s.oninput=ks.oninput=draw;draw();return()=>{};
}
