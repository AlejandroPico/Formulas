export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-gelu-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>entrada x</span><input id="x" type="range" min="-4" max="4" step="0.1" value="-1"><output>-1.0</output></label><label><span>comparar con</span><select id="cmp"><option value="relu">ReLU</option><option value="tanh">tanh</option><option value="none">ninguna</option></select></label><label><span>mostrar derivada</span><input id="der" type="range" min="0" max="1" step="1" value="1"><output>sí</output></label>';
const xIn=controls.querySelector('#x'),cmp=controls.querySelector('#cmp'),der=controls.querySelector('#der'),outs=controls.querySelectorAll('output');
function gelu(x){return .5*x*(1+Math.tanh(Math.sqrt(2/Math.PI)*(x+.044715*x*x*x)))}function relu(x){return Math.max(0,x)}function tanhAct(x){return Math.tanh(x)}
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const xv=+xIn.value,showD=+der.value>0,yv=gelu(xv),eps=.001,grad=(gelu(xv+eps)-gelu(xv-eps))/(2*eps);outs[0].textContent=xv.toFixed(1);outs[1].textContent=showD?'sí':'no';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('GELU: activación suave usada en Transformers',24,36);ctx.font='14px monospace';ctx.fillText('GELU(x)=xΦ(x)≈0.5x[1+tanh(√(2/π)(x+0.044715x³))]',24,64);const cx=W/2,cy=H-105,sc=62;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(55,cy);ctx.lineTo(W-55,cy);ctx.moveTo(cx,78);ctx.lineTo(cx,H-58);ctx.stroke();if(cmp.value!=='none'){ctx.setLineDash([5,5]);ctx.strokeStyle='#64748b';ctx.beginPath();for(let i=0;i<=260;i++){const x=-4+i/260*8,y=cmp.value==='relu'?relu(x):tanhAct(x),px=cx+x*sc,py=cy-y*sc;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();ctx.setLineDash([]);badge(cmp.value+' ref.',W*.70,120,'#cbd5e1')}ctx.strokeStyle='#8b5cf6';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=280;i++){const x=-4+i/280*8,px=cx+x*sc,py=cy-gelu(x)*sc;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();if(showD){ctx.strokeStyle='#f59e0b';ctx.lineWidth=2;ctx.beginPath();for(let i=0;i<=220;i++){const x=-4+i/220*8,d=(gelu(x+eps)-gelu(x-eps))/(2*eps),px=cx+x*sc,py=cy-d*55;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();badge('derivada',W*.70,150,'#fde68a')}const px=cx+xv*sc,py=cy-yv*sc;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();badge('GELU='+yv.toFixed(3),px+10,py-8,'#ddd6fe');readout.innerHTML='<b>GELU(x)='+yv.toFixed(4)+' · derivada≈'+grad.toFixed(4)+'</b><br>GELU no corta el eje negativo: lo pondera suavemente con una puerta gaussiana, por eso suele ser más estable que ReLU en modelos profundos.'}
xIn.oninput=der.oninput=draw;cmp.onchange=draw;draw();return()=>{};
}
