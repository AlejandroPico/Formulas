export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-gelu-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>entrada x</span><input id="x" type="range" min="-4" max="4" step="0.1" value="-1"><output>-1.0</output></label><label><span>comparar ReLU</span><input id="mix" type="range" min="0" max="1" step="1" value="1"><output>sí</output></label>';
const xIn=controls.querySelector('#x'),mix=controls.querySelector('#mix'),outs=controls.querySelectorAll('output');
function gelu(x){return .5*x*(1+Math.tanh(Math.sqrt(2/Math.PI)*(x+.044715*x*x*x)))}
function relu(x){return Math.max(0,x)}function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const xv=+xIn.value,show=+mix.value>0,yv=gelu(xv),eps=.001,grad=(gelu(xv+eps)-gelu(xv-eps))/(2*eps);outs[0].textContent=xv.toFixed(1);outs[1].textContent=show?'sí':'no';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('GELU: puerta suave gaussiana',24,36);ctx.font='14px monospace';ctx.fillText('GELU(x)≈0.5x[1+tanh(√(2/π)(x+0.044715x³))]',24,64);const cx=W/2,cy=H-95,sc=60;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(55,cy);ctx.lineTo(W-55,cy);ctx.moveTo(cx,80);ctx.lineTo(cx,H-55);ctx.stroke();if(show){ctx.setLineDash([5,5]);ctx.strokeStyle='#64748b';ctx.beginPath();for(let i=0;i<=240;i++){const x=-4+i/240*8,px=cx+x*sc,py=cy-relu(x)*sc;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();ctx.setLineDash([]);badge('ReLU ref.',W*.70,120,'#cbd5e1')}ctx.strokeStyle='#8b5cf6';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const x=-4+i/260*8,px=cx+x*sc,py=cy-gelu(x)*sc;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();const px=cx+xv*sc,py=cy-yv*sc;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();badge('GELU='+yv.toFixed(3),px+10,py-8,'#ddd6fe');readout.innerHTML='<b>GELU(x)='+yv.toFixed(4)+' · derivada≈'+grad.toFixed(4)+'</b><br>GELU deja pasar entradas positivas y atenúa negativas de forma suave, reduciendo cortes bruscos respecto a ReLU.'}
xIn.oninput=mix.oninput=draw;draw();return()=>{};
}
