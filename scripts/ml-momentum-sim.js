export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-momentum-sim');root.style.setProperty('--equation-color','#ea580c');
const ctx=canvas.getContext('2d');let theta=-3.5,vel=0,raf=0,trail=[];
controls.innerHTML='<label><span>momento γ</span><input id="g" type="range" min="0.50" max="0.98" step="0.04" value="0.90"><output>0.90</output></label><label><span>learning rate η</span><input id="eta" type="range" min="0.005" max="0.050" step="0.005" value="0.025"><output>0.025</output></label><button type="button" id="reset">reiniciar partícula</button>';
const gamma=controls.querySelector('#g'),eta=controls.querySelector('#eta'),outs=controls.querySelectorAll('output'),reset=controls.querySelector('#reset');
function cost(x){return Math.cos(2*x)*15+x*x*5}function grad(x){return -Math.sin(2*x)*30+x*10}
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function draw(){const g=+gamma.value,e=+eta.value;outs[0].textContent=g.toFixed(2);outs[1].textContent=e.toFixed(3);const gr=grad(theta);vel=g*vel+e*gr;theta-=vel;theta=Math.max(-4.4,Math.min(4.4,theta));trail.push(theta);if(trail.length>90)trail.shift();const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Momentum: descenso con inercia acumulada',24,36);ctx.font='14px monospace';ctx.fillText('v_t=γv_{t−1}+η∇J(θ),   θ←θ−v_t',24,64);const cx=W/2,cy=H-90,sx=W/9;ctx.strokeStyle='#334155';ctx.lineWidth=1.5;ctx.beginPath();for(let px=0;px<W;px++){const x=(px/W-.5)*8,y=cy-cost(x);if(px)ctx.lineTo(px,y);else ctx.moveTo(px,y)}ctx.stroke();ctx.strokeStyle='rgba(234,88,12,.35)';ctx.lineWidth=2;ctx.beginPath();trail.forEach((p,i)=>{const x=cx+p*sx,y=cy-cost(p);if(i)ctx.lineTo(x,y);else ctx.moveTo(x,y)});ctx.stroke();const px=cx+theta*sx,py=cy-cost(theta);ctx.fillStyle='#ea580c';ctx.beginPath();ctx.arc(px,py,8,0,7);ctx.fill();ctx.strokeStyle='#fbbf24';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(px-vel*70,py);ctx.stroke();ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.68,115,W*.27,128);ctx.strokeStyle='#334155';ctx.strokeRect(W*.68,115,W*.27,128);ctx.fillStyle='#fed7aa';ctx.font='13px monospace';ctx.fillText('θ='+theta.toFixed(3),W*.70,148);ctx.fillText('v='+vel.toFixed(3),W*.70,178);ctx.fillText('∇J='+gr.toFixed(2),W*.70,208);readout.innerHTML='<b>θ='+theta.toFixed(4)+' · v_t='+vel.toFixed(4)+' · ∇J='+gr.toFixed(2)+'</b><br>La traza muestra la memoria de la partícula. γ alto acelera en pendientes persistentes, pero puede sobrepasar mínimos.';raf=requestAnimationFrame(draw)}
reset.onclick=()=>{theta=-3.5;vel=0;trail=[]};draw();return()=>cancelAnimationFrame(raf);
}
