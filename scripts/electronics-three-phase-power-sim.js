export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','electronics-3phase-sim');root.style.setProperty('--equation-color','#0284c7');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>voltaje línea VL</span><input id="vl" type="range" min="380" max="440" step="10" value="400"><output>400 V</output></label><label><span>corriente línea IL</span><input id="il" type="range" min="5" max="80" step="5" value="25"><output>25 A</output></label><label><span>factor potencia</span><input id="fp" type="range" min="50" max="100" step="2" value="85"><output>0.85</output></label>';
const vlI=controls.querySelector('#vl'),ilI=controls.querySelector('#il'),fpI=controls.querySelector('#fp'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function wave(cx,cy,phase,c,W){ctx.strokeStyle=c;ctx.lineWidth=2;ctx.beginPath();for(let x=0;x<=W*.54;x++){const y=cy-Math.sin(x/34+phase)*42;if(x)ctx.lineTo(cx+x,y);else ctx.moveTo(cx+x,y)}ctx.stroke()}
function draw(){const VL=+vlI.value,IL=+ilI.value,fp=+fpI.value/100,S=Math.sqrt(3)*VL*IL/1000,P=S*fp,Q=Math.sqrt(Math.max(0,S*S-P*P));outs[0].textContent=VL+' V';outs[1].textContent=IL+' A';outs[2].textContent=fp.toFixed(2);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Potencia trifásica equilibrada',24,36);ctx.font='14px monospace';ctx.fillText('P=√3·VL·IL·cosφ ; S=√3·VL·IL',24,64);wave(70,150,0,'#0284c7',W);wave(70,150,2*Math.PI/3,'#f59e0b',W);wave(70,150,4*Math.PI/3,'#dc2626',W);tag('fases 120° equilibradas',82,235,'#bae6fd');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.63,110,W*.32,190);ctx.strokeStyle='#334155';ctx.strokeRect(W*.63,110,W*.32,190);tag('S='+S.toFixed(2)+' kVA',W*.65,150,'#e2e8f0');tag('P='+P.toFixed(2)+' kW',W*.65,188,'#bae6fd');tag('Q='+Q.toFixed(2)+' kvar',W*.65,226,'#fde68a');tag('por fase≈'+(P/3).toFixed(2)+' kW',W*.65,264,'#bbf7d0');readout.innerHTML='<b>P='+P.toFixed(2)+' kW · S='+S.toFixed(2)+' kVA · Q='+Q.toFixed(2)+' kvar</b><br>En un sistema equilibrado, las tres fases transportan la misma potencia y están desfasadas 120°.'}
vlI.oninput=ilI.oninput=fpI.oninput=draw;draw();return()=>{};
}
