export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','transformer-pe-sim');root.style.setProperty('--equation-color','#ec4899');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>posición pos</span><input id="pos" type="range" min="0" max="80" step="1" value="5"><output>5</output></label><label><span>d_model</span><input id="d" type="range" min="32" max="128" step="32" value="64"><output>64</output></label>';
const posI=controls.querySelector('#pos'),dI=controls.querySelector('#d'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function pe(pos,i,d){const div=Math.pow(10000,2*Math.floor(i/2)/d);return i%2===0?Math.sin(pos/div):Math.cos(pos/div)}
function draw(){const pos=+posI.value,d=+dI.value;outs[0].textContent=pos;outs[1].textContent=d;const[W,H]=size(),x0=70,y0=H*.52,w=W*.74,h=110;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Codificación posicional sinusoidal',24,36);ctx.font='14px monospace';ctx.fillText('PE(pos,2i)=sin(pos/10000^(2i/d)); PE(pos,2i+1)=cos(...)',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#ec4899';ctx.lineWidth=3;ctx.beginPath();for(let px=0;px<=w;px++){const i=Math.floor(px/w*(d-1)),v=pe(pos,i,d),x=x0+px,y=y0-v*h/2;if(px)ctx.lineTo(x,y);else ctx.moveTo(x,y)}ctx.stroke();for(let i=0;i<d;i+=Math.max(1,Math.floor(d/16))){const v=pe(pos,i,d),x=x0+i/(d-1)*w,y=y0-v*h/2;ctx.fillStyle=v>=0?'#f9a8d4':'#7dd3fc';ctx.fillRect(x-3,y-3,6,6)}tag('+1',28,y0-h/2+5,'#cbd5e1');tag('-1',28,y0+h/2+5,'#cbd5e1');tag('dimensiones bajas: ondas rápidas',x0+8,H-92,'#fbcfe8');tag('dimensiones altas: ondas largas',x0+w-225,H-92,'#bae6fd');readout.innerHTML='<b>pos='+pos+' · d_model='+d+'</b><br>Las componentes seno/coseno crean una firma de posición con varias frecuencias, útil para extrapolar relaciones relativas.'}
posI.oninput=dI.oninput=draw;draw();return()=>{};
}
