export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-perplexity-sim');root.style.setProperty('--equation-color','#ea580c');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>prob. token correcto</span><input id="p" type="range" min="1" max="99" step="1" value="25"><output>25%</output></label><label><span>longitud secuencia</span><input id="len" type="range" min="1" max="20" step="1" value="8"><output>8</output></label>';
const p=controls.querySelector('#p'),len=controls.querySelector('#len'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const pp=+p.value/100,L=+len.value,H=-Math.log2(pp),PPL=Math.pow(2,H),seqLoss=L*H;outs[0].textContent=(pp*100).toFixed(0)+'%';outs[1].textContent=L;const[W,Hh]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,Hh);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Perplejidad: cuántas opciones “equivalentes” ve el modelo',24,36);ctx.font='14px monospace';ctx.fillText('PPL = 2^H = exp(cross entropy)',24,64);const x=70,y=125,w=W*.55,h=Hh*.44;ctx.fillStyle='rgba(234,88,12,.10)';ctx.fillRect(x,y,w,h);ctx.strokeStyle='#334155';ctx.strokeRect(x,y,w,h);const n=Math.min(40,Math.ceil(PPL)),cols=10;for(let i=0;i<n;i++){ctx.fillStyle=i===0?'#ea580c':'#475569';ctx.fillRect(x+25+(i%cols)*32,y+28+Math.floor(i/cols)*32,20,20)}badge('opciones equivalentes ≈ '+PPL.toFixed(2),x+22,y+h-18,'#fed7aa');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.67,130,W*.27,150);ctx.strokeStyle='#334155';ctx.strokeRect(W*.67,130,W*.27,150);badge('H token = '+H.toFixed(3)+' bits',W*.69,165,'#fed7aa');badge('PPL = '+PPL.toFixed(2),W*.69,198,'#fdba74');badge('loss secuencia = '+seqLoss.toFixed(2),W*.69,232,'#e2e8f0');readout.innerHTML='<b>H='+H.toFixed(3)+' bits/token · PPL='+PPL.toFixed(2)+'</b><br>Una PPL baja indica que el modelo concentra probabilidad en el token correcto; PPL alta equivale a elegir entre muchas alternativas plausibles.'}
p.oninput=len.oninput=draw;draw();return()=>{};
}
