export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-ln-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>neurona 1</span><input id="n1" type="range" min="-12" max="12" step="1" value="-6"><output>-6</output></label><label><span>neurona 2</span><input id="n2" type="range" min="-12" max="12" step="1" value="10"><output>10</output></label><label><span>neurona 3</span><input id="n3" type="range" min="-12" max="12" step="1" value="-1"><output>-1</output></label><label><span>β desplazamiento</span><input id="be" type="range" min="-2" max="2" step="0.1" value="0"><output>0.0</output></label>';
const ns=[...controls.querySelectorAll('input')].slice(0,3),be=controls.querySelector('#be'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function drawBar(x,y,v,c,label){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,220,22);const mid=x+110;ctx.fillStyle=c;ctx.fillRect(v>=0?mid:mid+v*18,y,Math.abs(v*18),22);badge(label+' '+v.toFixed(2),x+235,y+16,c)}
function draw(){const vals=ns.map(n=>+n.value),beta=+be.value;ns.forEach((n,i)=>outs[i].textContent=n.value);outs[3].textContent=beta.toFixed(1);const mean=vals.reduce((a,c)=>a+c,0)/vals.length,varr=vals.reduce((a,c)=>a+(c-mean)**2,0)/vals.length,sd=Math.sqrt(varr+1e-5),hat=vals.map(x=>(x-mean)/sd+beta);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Layer Normalization: normalizar dentro de cada muestra',24,36);ctx.font='14px monospace';ctx.fillText('x̂_i=(x_i−μ_L)/√(σ_L²+ε),  sobre características de una muestra',24,64);badge('activaciones de una muestra',70,112,'#ddd6fe');vals.forEach((v,i)=>drawBar(70,135+i*42,v,'#8b5cf6','n'+(i+1)));badge('normalizadas por capa',70,295,'#c4b5fd');hat.forEach((v,i)=>drawBar(70,318+i*34,v,'#a78bfa','n̂'+(i+1)));ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.63,130,W*.31,180);ctx.strokeStyle='#334155';ctx.strokeRect(W*.63,130,W*.31,180);badge('μ_L='+mean.toFixed(2),W*.65,165,'#ddd6fe');badge('σ²_L='+varr.toFixed(2),W*.65,198,'#ddd6fe');badge('independiente del batch',W*.65,236,'#bbf7d0');badge('β='+beta.toFixed(1),W*.65,270,'#e9d5ff');readout.innerHTML='<b>μ_L='+mean.toFixed(2)+' · σ²_L='+varr.toFixed(2)+'</b><br>LayerNorm usa las características de una sola muestra. Por eso se comporta igual en entrenamiento e inferencia y funciona bien en Transformers.'}
[...ns,be].forEach(x=>x.oninput=draw);draw();return()=>{};
}
