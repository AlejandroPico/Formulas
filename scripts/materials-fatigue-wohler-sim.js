export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','materials-fatigue-sim');root.style.setProperty('--equation-color','#dc2626');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>esfuerzo alternante S</span><input id="s" type="range" min="150" max="450" step="10" value="320"><output>320 MPa</output></label><label><span>límite endurancia Se</span><input id="se" type="range" min="160" max="260" step="10" value="200"><output>200 MPa</output></label>';
const sI=controls.querySelector('#s'),seI=controls.querySelector('#se'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function life(S,Se){if(S<=Se)return Infinity;const ratio=(450-S)/(450-Se);return Math.pow(10,3+Math.max(0,Math.min(1,ratio))*3.5)}
function draw(){const S=+sI.value,Se=+seI.value,N=life(S,Se);outs[0].textContent=S+' MPa';outs[1].textContent=Se+' MPa';const[W,H]=size(),x0=70,y0=H-85,w=W*.62,h=H*.56;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Fatiga de materiales: curva de Wöhler S-N',24,36);ctx.font='14px monospace';ctx.fillText('S = A·N^b ; vida finita sobre el límite de endurancia',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#94a3b8';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(x0,y0-Se/500*h);ctx.lineTo(x0+w,y0-Se/500*h);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#dc2626';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=280;i++){const t=i/280,sval=Math.max(Se,450-t*300*Math.pow(Math.max(t,.001),.2)),px=x0+t*w,py=y0-sval/500*h;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();const ratio=S<=Se?1:(450-S)/(450-Se),px=x0+Math.max(0,Math.min(1,ratio))*w,py=y0-S/500*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag(S<=Se?'vida infinita':'N≈'+N.toLocaleString('es-ES',{maximumFractionDigits:0}),px+10,py-8,S<=Se?'#bbf7d0':'#fecaca');tag('Se '+Se+' MPa',x0+w-82,y0-Se/500*h-8,'#cbd5e1');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.68,125,W*.27,165);ctx.strokeStyle='#334155';ctx.strokeRect(W*.68,125,W*.27,165);tag('S='+S+' MPa',W*.70,162,'#fecaca');tag('Se='+Se+' MPa',W*.70,198,'#e2e8f0');tag(S<=Se?'régimen seguro':'daño acumulativo',W*.70,236,S<=Se?'#bbf7d0':'#fecaca');readout.innerHTML='<b>Vida estimada: '+(S<=Se?'∞':N.toLocaleString('es-ES',{maximumFractionDigits:0})+' ciclos')+'</b><br>La fatiga permite fallos por debajo de la resistencia estática: el número de ciclos cae de forma logarítmica al subir S.'}
sI.oninput=seI.oninput=draw;draw();return()=>{};
}
