export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','transformer-rope-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>posición m</span><input id="m" type="range" min="0" max="16" step="1" value="3"><output>3</output></label><label><span>θ base</span><input id="th" type="range" min="10" max="80" step="5" value="24"><output>24°</output></label>';
const mi=controls.querySelector('#m'),thi=controls.querySelector('#th'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function arrow(x,y,dx,dy,c){ctx.strokeStyle=c;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+dx,y-dy);ctx.stroke();ctx.fillStyle=c;ctx.beginPath();ctx.arc(x+dx,y-dy,5,0,7);ctx.fill()}
function draw(){const m=+mi.value,theta=+thi.value*Math.PI/180,phi=m*theta,R=72,dx=R*Math.cos(phi),dy=R*Math.sin(phi);outs[0].textContent=m;outs[1].textContent=(+thi.value)+'°';const[W,H]=size(),cx=W*.45,cy=H*.55;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('RoPE: posición como rotación de fase',24,36);ctx.font='14px monospace';ctx.fillText('R_{θ,m} v rota pares de dimensiones por ángulo m·θ',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(cx-150,cy);ctx.lineTo(cx+150,cy);ctx.moveTo(cx,cy-130);ctx.lineTo(cx,cy+130);ctx.stroke();ctx.strokeStyle='#1e293b';ctx.beginPath();ctx.arc(cx,cy,R,0,7);ctx.stroke();arrow(cx,cy,R,0,'#64748b');arrow(cx,cy,dx,dy,'#8b5cf6');ctx.strokeStyle='#fbbf24';ctx.beginPath();ctx.arc(cx,cy,36,0,-phi,true);ctx.stroke();tag('base',cx+R+8,cy+5,'#cbd5e1');tag('pos '+m,cx+dx+10,cy-dy,'#ddd6fe');tag('fase '+(phi*180/Math.PI).toFixed(0)+'°',cx+42,cy-42,'#fde68a');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.28,150);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.28,150);tag('norma preservada',W*.68,165,'#bbf7d0');tag('posición relativa',W*.68,202,'#ddd6fe');tag('fase acumulada',W*.68,238,'#e2e8f0');readout.innerHTML='<b>m='+m+' · mθ='+(phi*180/Math.PI).toFixed(1)+'°</b><br>RoPE no suma una etiqueta posicional: rota las componentes, preserva norma y codifica desplazamientos relativos en la fase.'}
mi.oninput=thi.oninput=draw;draw();return()=>{};
}
