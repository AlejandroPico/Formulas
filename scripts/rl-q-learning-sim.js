export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','rl-qlearning-sim');root.style.setProperty('--equation-color','#10b981');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>recompensa r</span><input id="r" type="range" min="-10" max="20" step="2" value="10"><output>+10</output></label><label><span>max Q(s′,a′)</span><input id="mq" type="range" min="0" max="50" step="5" value="30"><output>30</output></label><label><span>α aprendizaje</span><input id="a" type="range" min="0.05" max="0.60" step="0.05" value="0.20"><output>0.20</output></label>';
const rI=controls.querySelector('#r'),mI=controls.querySelector('#mq'),aI=controls.querySelector('#a'),outs=controls.querySelectorAll('output');const gamma=.9,Q=15;
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c,m){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,24);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,Math.abs(v)/m)),24);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText(l+': '+v.toFixed(2),x+8,y+16)}
function draw(){const r=+rI.value,maxQ=+mI.value,alpha=+aI.value,target=r+gamma*maxQ,td=target-Q,nQ=Q+alpha*td;outs[0].textContent=(r>=0?'+':'')+r;outs[1].textContent=maxQ;outs[2].textContent=alpha.toFixed(2);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Q-Learning: objetivo greedy fuera de la política',24,36);ctx.font='14px monospace';ctx.fillText('Q←Q+α[r+γ max_a Q(s′,a)−Q]',24,64);bar(70,125,W*.55,'Q previo',Q,'#64748b',60);bar(70,170,W*.55,'target TD',target,'#10b981',70);bar(70,215,W*.55,'error TD',td,'#f59e0b',60);bar(70,260,W*.55,'nuevo Q',nQ,'#22c55e',70);ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.28,150);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.28,150);badge('off-policy',W*.68,162,'#bbf7d0');badge('elige max futuro',W*.68,198,'#bbf7d0');badge('γ = '+gamma.toFixed(2),W*.68,234,'#e2e8f0');readout.innerHTML='<b>target='+target.toFixed(2)+' · TD='+td.toFixed(2)+' · Q nuevo='+nQ.toFixed(3)+'</b><br>Q-Learning aprende el valor de la acción óptima futura aunque el agente haya explorado otra acción real.'}
rI.oninput=mI.oninput=aI.oninput=draw;draw();return()=>{};
}
