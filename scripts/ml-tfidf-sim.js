export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-tfidf-sim');root.style.setProperty('--equation-color','#ec4899');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>TF apariciones</span><input id="tf" type="range" min="1" max="20" step="1" value="4"><output>4</output></label><label><span>DF docs con término</span><input id="df" type="range" min="1" max="100" step="1" value="5"><output>5</output></label><label><span>tamaño corpus N</span><input id="N" type="range" min="20" max="500" step="20" value="100"><output>100</output></label>';
const tf=controls.querySelector('#tf'),df=controls.querySelector('#df'),N=controls.querySelector('#N'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c,m){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,24);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,v/m)),24);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText(l+': '+v.toFixed(3),x+8,y+16)}
function draw(){const T=+tf.value,D=+df.value,NN=+N.value,IDF=Math.log10(NN/Math.min(D,NN)),score=T*IDF;outs[0].textContent=T;outs[1].textContent=D;outs[2].textContent=NN;const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('TF-IDF: término frecuente aquí, raro en el corpus',24,36);ctx.font='14px monospace';ctx.fillText('TF-IDF = TF · log10(N/DF)',24,64);const x=62,y=120,w=W*.55,h=H*.45;ctx.fillStyle='rgba(236,72,153,.10)';ctx.fillRect(x,y,w,h);ctx.strokeStyle='#334155';ctx.strokeRect(x,y,w,h);const cols=20,rows=5,total=cols*rows,active=Math.min(total,Math.round(D/NN*total));for(let i=0;i<total;i++){const px=x+14+(i%cols)*(w-28)/cols,py=y+18+Math.floor(i/cols)*(h-36)/rows;ctx.fillStyle=i<active?'#ec4899':'#334155';ctx.fillRect(px,py,10,10)}badge('documentos con término: '+D+'/'+NN,x+16,y+h-12,'#fbcfe8');bar(W*.67,135,W*.27,'TF',T,'#f472b6',20);bar(W*.67,175,W*.27,'IDF',IDF,'#ec4899',Math.log10(500));bar(W*.67,215,W*.27,'TF-IDF',score,'#f9a8d4',40);readout.innerHTML='<b>TF='+T+' · IDF='+IDF.toFixed(3)+' · TF-IDF='+score.toFixed(3)+'</b><br>Si DF sube, el término aparece en demasiados documentos y pierde poder discriminante aunque su TF local sea alto.'}
tf.oninput=df.oninput=N.oninput=draw;draw();return()=>{};
}
