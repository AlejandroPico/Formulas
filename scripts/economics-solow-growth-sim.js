export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','econ-solow-sim');root.style.setProperty('--equation-color','#6366f1');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>ahorro s</span><input id="s" type="range" min="8" max="45" step="1" value="20"><output>20%</output></label><label><span>dilución n+g+d</span><input id="d" type="range" min="4" max="16" step="1" value="8"><output>8%</output></label><label><span>alpha</span><input id="a" type="range" min="25" max="45" step="1" value="33"><output>0.33</output></label>';
const sI=controls.querySelector('#s'),dI=controls.querySelector('#d'),aI=controls.querySelector('#a'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const s=+sI.value/100,dil=+dI.value/100,a=+aI.value/100,k=Math.pow(s/dil,1/(1-a)),y=Math.pow(k,a),c=(1-s)*y;outs[0].textContent=(s*100).toFixed(0)+'%';outs[1].textContent=(dil*100).toFixed(0)+'%';outs[2].textContent=a.toFixed(2);const[W,H]=size(),x0=72,y0=H-82,w=W*.62,h=H*.56,kMax=Math.max(8,k*1.5);ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Solow: estado estacionario del capital por trabajador',24,36);ctx.font='14px monospace';ctx.fillText('k*=[s/(n+g+δ)]^(1/(1−α)), y*=k*^α',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#6366f1';ctx.lineWidth=3;ctx.beginPath();for(let i=1;i<=240;i++){const kk=i/240*kMax,sy=s*Math.pow(kk,a),px=x0+kk/kMax*w,py=y0-sy/Math.max(.01,s*Math.pow(kMax,a))*h*.85;if(i>1)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();ctx.strokeStyle='#f59e0b';ctx.beginPath();ctx.moveTo(x0,y0);ctx.lineTo(x0+w,y0-dil*kMax/(s*Math.pow(kMax,a))*h*.85);ctx.stroke();const px=x0+k/kMax*w,py=y0-dil*k/(s*Math.pow(kMax,a))*h*.85;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag('k* '+k.toFixed(2),px+10,py-8,'#c7d2fe');tag('s·f(k)',x0+20,y0-h*.60,'#c7d2fe');tag('(n+g+δ)k',x0+w*.55,y0-h*.32,'#fde68a');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.67,125,W*.28,185);ctx.strokeStyle='#334155';ctx.strokeRect(W*.67,125,W*.28,185);tag('k*='+k.toFixed(3),W*.69,162,'#c7d2fe');tag('y*='+y.toFixed(3),W*.69,198,'#e2e8f0');tag('c*='+c.toFixed(3),W*.69,234,'#bbf7d0');tag('α='+a.toFixed(2),W*.69,270,'#cbd5e1');readout.innerHTML='<b>k*='+k.toFixed(4)+' · y*='+y.toFixed(4)+' · c*='+c.toFixed(4)+'</b><br>Un mayor ahorro eleva el nivel estacionario de producción por trabajador, pero el crecimiento sostenido de largo plazo depende del progreso tecnológico.'}
sI.oninput=dI.oninput=aI.oninput=draw;draw();return()=>{};
}
