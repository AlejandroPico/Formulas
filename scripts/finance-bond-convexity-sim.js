export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','finance-convexity-sim');root.style.setProperty('--equation-color','#2563eb');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>shock Delta y</span><input id="dy" type="range" min="-300" max="300" step="25" value="200"><output>+2.00%</output></label><label><span>duración mod.</span><input id="d" type="range" min="300" max="950" step="25" value="650"><output>6.50</output></label><label><span>convexidad C</span><input id="c" type="range" min="10" max="90" step="5" value="55"><output>55</output></label>';
const dyI=controls.querySelector('#dy'),dI=controls.querySelector('#d'),cI=controls.querySelector('#c'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const dy=+dyI.value/10000,D=+dI.value/100,C=+cI.value,lineal=-D*dy,conv=.5*C*dy*dy,total=lineal+conv;outs[0].textContent=(dy>=0?'+':'')+(dy*100).toFixed(2)+'%';outs[1].textContent=D.toFixed(2);outs[2].textContent=C.toFixed(0);const[W,H]=size(),x0=72,y0=H*.56,w=W*.60,h=H*.45;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Convexidad de bonos: corrección de segundo orden',24,36);ctx.font='14px monospace';ctx.fillText('Delta P/P ≈ -Dmod·Delta y + 0.5·C·(Delta y)^2',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0+h*.55);ctx.moveTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#94a3b8';ctx.lineWidth=2;ctx.beginPath();for(let i=0;i<=260;i++){const x=-.03+i/260*.06,y=-D*x,px=x0+(x+.03)/.06*w,py=y0-y*7*h;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();ctx.strokeStyle='#2563eb';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const x=-.03+i/260*.06,y=-D*x+.5*C*x*x,px=x0+(x+.03)/.06*w,py=y0-y*7*h;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();const px=x0+(dy+.03)/.06*w,py=y0-total*7*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag((total*100).toFixed(2)+'%',px+10,py-8,total>=0?'#bbf7d0':'#fecaca');tag('lineal',x0+w*.66,y0-(-D*.012)*7*h+14,'#cbd5e1');tag('convexa',x0+w*.66,y0-(-D*.012+.5*C*.012*.012)*7*h-12,'#bfdbfe');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.67,125,W*.28,185);ctx.strokeStyle='#334155';ctx.strokeRect(W*.67,125,W*.28,185);tag('lineal='+(lineal*100).toFixed(2)+'%',W*.69,162,lineal>=0?'#bbf7d0':'#fecaca');tag('convexidad=+'+(conv*100).toFixed(3)+'%',W*.69,198,'#bfdbfe');tag('neto='+(total*100).toFixed(2)+'%',W*.69,234,total>=0?'#bbf7d0':'#fecaca');tag('C='+C.toFixed(0),W*.69,270,'#e2e8f0');readout.innerHTML='<b>Impacto neto='+(total*100).toFixed(3)+'% · ajuste convexidad=+'+(conv*100).toFixed(3)+'%</b><br>La convexidad añade valor simétrico: suaviza pérdidas cuando suben tipos y amplifica ganancias cuando bajan.'}
dyI.oninput=dI.oninput=cI.oninput=draw;draw();return()=>{};
}
