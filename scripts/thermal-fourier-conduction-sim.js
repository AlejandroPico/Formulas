export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','thermal-fourier-sim');root.style.setProperty('--equation-color','#ea580c');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>conductividad k</span><input id="k" type="range" min="10" max="220" step="10" value="50"><output>50 W/mK</output></label><label><span>T caliente</span><input id="th" type="range" min="50" max="180" step="5" value="100"><output>100 °C</output></label><label><span>espesor L</span><input id="l" type="range" min="10" max="60" step="5" value="25"><output>25 cm</output></label>';
const kI=controls.querySelector('#k'),thI=controls.querySelector('#th'),lI=controls.querySelector('#l'),outs=controls.querySelectorAll('output');const A=2,Tc=20;
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(420,Math.floor(b.height||540));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const k=+kI.value,Th=+thI.value,L=+lI.value/100,dT=Th-Tc,Q=k*A*dT/L,R=L/(k*A);outs[0].textContent=k+' W/mK';outs[1].textContent=Th+' °C';outs[2].textContent=(L*100).toFixed(0)+' cm';const[W,H]=size(),x0=80,y0=145,w=W*.58,h=110;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Conducción de Fourier 1D en estado estacionario',24,36);ctx.font='14px monospace';ctx.fillText('Q = kAΔT/L ; Rth=L/(kA)',24,64);const grad=ctx.createLinearGradient(x0,0,x0+w,0);grad.addColorStop(0,'#ef4444');grad.addColorStop(1,'#3b82f6');ctx.fillStyle=grad;ctx.fillRect(x0,y0,w,h);ctx.strokeStyle='#334155';ctx.lineWidth=2;ctx.strokeRect(x0,y0,w,h);ctx.strokeStyle='#f8fafc';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x0,y0+h-(Th/200)*h);ctx.lineTo(x0+w,y0+h-(Tc/200)*h);ctx.stroke();for(let i=0;i<6;i++){const x=x0+30+i*45;ctx.strokeStyle='rgba(248,250,252,.65)';ctx.beginPath();ctx.moveTo(x,y0+h+22);ctx.lineTo(x+24,y0+h+22);ctx.stroke()}tag('caliente '+Th+'°C',x0,y0-12,'#fecaca');tag('frío '+Tc+'°C',x0+w-68,y0-12,'#bfdbfe');tag('flujo Q →',x0+35,y0+h+45,'#fed7aa');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.68,125,W*.27,165);ctx.strokeStyle='#334155';ctx.strokeRect(W*.68,125,W*.27,165);tag('ΔT='+dT.toFixed(0)+' K',W*.70,162,'#fed7aa');tag('Rth='+R.toFixed(4)+' K/W',W*.70,198,'#e2e8f0');tag('Q='+(Q/1000).toFixed(2)+' kW',W*.70,236,'#fdba74');readout.innerHTML='<b>Q='+(Q/1000).toFixed(3)+' kW · Rth='+R.toFixed(5)+' K/W</b><br>En régimen estacionario 1D, el perfil térmico es lineal y el flujo aumenta con k, área y salto térmico, pero disminuye con el espesor.'}
kI.oninput=thI.oninput=lI.oninput=draw;draw();return()=>{};
}
