export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','mechanics-mohr-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>σx</span><input id="sx" type="range" min="10" max="140" step="5" value="100"><output>100 MPa</output></label><label><span>σy</span><input id="sy" type="range" min="-60" max="80" step="5" value="30"><output>30 MPa</output></label><label><span>τxy</span><input id="t" type="range" min="0" max="70" step="5" value="30"><output>30 MPa</output></label>';
const sxI=controls.querySelector('#sx'),syI=controls.querySelector('#sy'),tI=controls.querySelector('#t'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(440,Math.floor(b.height||570));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const sx=+sxI.value,sy=+syI.value,t=+tI.value,c=(sx+sy)/2,R=Math.hypot((sx-sy)/2,t),s1=c+R,s2=c-R,ang=.5*Math.atan2(2*t,sx-sy)*180/Math.PI;outs[0].textContent=sx+' MPa';outs[1].textContent=sy+' MPa';outs[2].textContent=t+' MPa';const[W,H]=size(),ox=W*.36,oy=H*.55,scale=Math.min(2.1,(W*.52)/(Math.max(1,s1+40)));ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Círculo de Mohr: transformación de tensiones 2D',24,36);ctx.font='14px monospace';ctx.fillText('R=√[((σx−σy)/2)²+τxy²],  σ1,2=σprom±R',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(40,oy);ctx.lineTo(W*.67,oy);ctx.moveTo(ox,90);ctx.lineTo(ox,H-60);ctx.stroke();const cx=ox+c*scale,r=R*scale;ctx.fillStyle='rgba(139,92,246,.08)';ctx.beginPath();ctx.arc(cx,oy,r,0,7);ctx.fill();ctx.strokeStyle='#8b5cf6';ctx.lineWidth=3;ctx.stroke();const px=ox+sx*scale,py=oy-t*scale,qx=ox+sy*scale,qy=oy+t*scale;ctx.strokeStyle='#94a3b8';ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(qx,qy);ctx.stroke();ctx.fillStyle='#f8fafc';[ [px,py],[qx,qy] ].forEach(p=>{ctx.beginPath();ctx.arc(p[0],p[1],6,0,7);ctx.fill()});tag('σ1 '+s1.toFixed(1),ox+s1*scale-28,oy-10,'#ddd6fe');tag('σ2 '+s2.toFixed(1),ox+s2*scale-28,oy+28,'#ddd6fe');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.68,125,W*.27,185);ctx.strokeStyle='#334155';ctx.strokeRect(W*.68,125,W*.27,185);tag('σprom='+c.toFixed(1),W*.70,162,'#e2e8f0');tag('R=τmax='+R.toFixed(1),W*.70,198,'#ddd6fe');tag('θp='+ang.toFixed(1)+'°',W*.70,234,'#cbd5e1');tag('σ1='+s1.toFixed(1),W*.70,270,'#ddd6fe');readout.innerHTML='<b>σ1='+s1.toFixed(2)+' MPa · σ2='+s2.toFixed(2)+' MPa · τmax='+R.toFixed(2)+' MPa</b><br>Mohr convierte la rotación física del elemento en un círculo: las tensiones principales aparecen donde τ se anula.'}
sxI.oninput=syI.oninput=tI.oninput=draw;draw();return()=>{};
}
