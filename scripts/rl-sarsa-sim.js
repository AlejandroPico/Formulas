export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','rl-sarsa-sim');root.style.setProperty('--equation-color','#ea580c');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>Q(s′,a′) acción real</span><input id="qa" type="range" min="-20" max="35" step="5" value="-10"><output>-10</output></label><label><span>recompensa r</span><input id="r" type="range" min="-10" max="20" step="2" value="4"><output>+4</output></label><label><span>α aprendizaje</span><input id="a" type="range" min="0.05" max="0.60" step="0.05" value="0.20"><output>0.20</output></label>';
const qI=controls.querySelector('#qa'),rI=controls.querySelector('#r'),aI=controls.querySelector('#a'),outs=controls.querySelectorAll('output');const gamma=.9,Q=15;
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c,m){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,24);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,Math.abs(v)/m)),24);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText(l+': '+v.toFixed(2),x+8,y+16)}
function draw(){const qn=+qI.value,r=+rI.value,alpha=+aI.value,target=r+gamma*qn,td=target-Q,nQ=Q+alpha*td;outs[0].textContent=qn;outs[1].textContent=(r>=0?'+':'')+r;outs[2].textContent=alpha.toFixed(2);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('SARSA: aprende desde la acción realmente tomada',24,36);ctx.font='14px monospace';ctx.fillText('Q←Q+α[r+γQ(s′,a′)−Q(s,a)]',24,64);bar(70,125,W*.55,'Q previo',Q,'#64748b',60);bar(70,170,W*.55,'Q(s′,a′)',qn,'#ea580c',40);bar(70,215,W*.55,'target on-policy',target,'#f59e0b',55);bar(70,260,W*.55,'nuevo Q',nQ,'#fb923c',60);ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.28,160);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.28,160);badge('on-policy',W*.68,162,'#fed7aa');badge('incluye exploración real',W*.68,198,'#fed7aa');badge(qn<0?'castiga ruta arriesgada':'refuerza ruta elegida',W*.68,238,qn<0?'#fecaca':'#bbf7d0');readout.innerHTML='<b>target='+target.toFixed(2)+' · TD='+td.toFixed(2)+' · Q nuevo='+nQ.toFixed(3)+'</b><br>SARSA actualiza con la acción que la política ejecutó de verdad; por eso incorpora el coste de explorar acciones malas.'}
qI.oninput=rI.oninput=aI.oninput=draw;draw();return()=>{};
}
