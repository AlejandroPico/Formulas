export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-sigmoid-sim');root.style.setProperty('--equation-color','#2563eb');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>peso w</span><input id="w" type="range" min="1" max="15" step="1" value="3"><output>3</output></label><label><span>sesgo b</span><input id="b" type="range" min="-5" max="5" step="0.5" value="0"><output>0.0</output></label><label><span>entrada x</span><input id="x" type="range" min="-3" max="3" step="0.1" value="0"><output>0.0</output></label>';
const wIn=controls.querySelector('#w'),bIn=controls.querySelector('#b'),xIn=controls.querySelector('#x'),outs=controls.querySelectorAll('output');
function sig(z){return 1/(1+Math.exp(-z))}function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const w=+wIn.value,b=+bIn.value,xv=+xIn.value,z=w*xv+b,p=sig(z),grad=p*(1-p),boundary=-b/w;outs[0].textContent=w;outs[1].textContent=b.toFixed(1);outs[2].textContent=xv.toFixed(1);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Regresión logística: probabilidad y frontera',24,36);ctx.font='14px monospace';ctx.fillText('σ(z)=1/(1+e^(−z)),  z=w·x+b,  σ′=σ(1−σ)',24,64);const ox=62,oy=H-86,ww=W*.60,hh=H*.56;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(ox,oy-hh);ctx.lineTo(ox,oy);ctx.lineTo(ox+ww,oy);ctx.stroke();ctx.setLineDash([5,5]);ctx.strokeStyle='#64748b';ctx.beginPath();ctx.moveTo(ox,oy-hh*.5);ctx.lineTo(ox+ww,oy-hh*.5);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#2563eb';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=280;i++){const xx=-3+i/280*6,yy=oy-sig(w*xx+b)*hh;if(i)ctx.lineTo(ox+i/280*ww,yy);else ctx.moveTo(ox,yy)}ctx.stroke();const bx=ox+(boundary+3)/6*ww;if(boundary>=-3&&boundary<=3){ctx.strokeStyle='#f59e0b';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(bx,oy);ctx.lineTo(bx,oy-hh);ctx.stroke();ctx.setLineDash([]);badge('frontera x='+boundary.toFixed(2),bx-48,oy-hh-8,'#fde68a')}const px=ox+(xv+3)/6*ww,py=oy-p*hh;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();badge('P='+p.toFixed(3),px+10,py-8,'#bfdbfe');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.69,128,W*.25,112);ctx.strokeStyle='#334155';ctx.strokeRect(W*.69,128,W*.25,112);badge('clase: '+(p>=.5?'1':'0'),W*.71,160,p>=.5?'#bbf7d0':'#fecaca');badge('incertidumbre: '+(4*grad).toFixed(2),W*.71,195,'#cbd5e1');readout.innerHTML='<b>z='+z.toFixed(3)+' · σ(z)='+p.toFixed(4)+' · σ′='+grad.toFixed(4)+'</b><br>El peso cambia la pendiente; el sesgo desplaza la frontera. La derivada cae en saturación, aunque la probabilidad sea muy segura.'}
wIn.oninput=bIn.oninput=xIn.oninput=draw;draw();return()=>{};
}
