export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','electronics-db-sim');root.style.setProperty('--equation-color','#ea580c');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>V entrada</span><input id="vin" type="range" min="1" max="10" step="0.5" value="2"><output>2.0 V</output></label><label><span>V salida</span><input id="vout" type="range" min="0.1" max="50" step="0.5" value="20"><output>20.0 V</output></label>';
const vin=controls.querySelector('#vin'),vout=controls.querySelector('#vout'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(380,Math.floor(b.height||520));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c,m){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,28);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,v/m)),28);tag(l+': '+v.toFixed(2),x+8,y+19,'#e2e8f0')}
function draw(){const Vi=+vin.value,Vo=+vout.value,g=Vo/Vi,db=20*Math.log10(g);outs[0].textContent=Vi.toFixed(1)+' V';outs[1].textContent=Vo.toFixed(1)+' V';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Ganancia de voltaje en decibelios',24,36);ctx.font='14px monospace';ctx.fillText('GdB = 20 log10(Vout/Vin)',24,64);bar(70,125,W*.55,'Vin',Vi,'#64748b',50);bar(70,175,W*.55,'Vout',Vo,'#ea580c',50);bar(70,225,W*.55,'ganancia lineal',g,'#f59e0b',25);ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.29,150);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.29,150);tag('G='+g.toFixed(2)+'x',W*.68,162,'#fed7aa');tag('GdB='+db.toFixed(2)+' dB',W*.68,198,db>=0?'#bbf7d0':'#fecaca');tag(db>=0?'amplificación':'atenuación',W*.68,236,db>=0?'#bbf7d0':'#fecaca');readout.innerHTML='<b>G='+g.toFixed(3)+'x · GdB='+db.toFixed(2)+' dB</b><br>La escala dB convierte relaciones multiplicativas de voltaje en una escala logarítmica compacta: 20 dB equivale a 10x.'}
vin.oninput=vout.oninput=draw;draw();return()=>{};
}
