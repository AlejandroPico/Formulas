export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-adam-sim');root.style.setProperty('--equation-color','#6366f1');
const ctx=canvas.getContext('2d');let m=0,v=0,t=0;
controls.innerHTML='<label><span>gradiente g_t</span><input id="g" type="range" min="-10" max="10" step="1" value="4"><output>4</output></label><button type="button" id="step">paso Adam</button><button type="button" id="reset">reiniciar momentos</button>';
const g=controls.querySelector('#g'),out=controls.querySelector('output'),stepBtn=controls.querySelector('#step'),reset=controls.querySelector('#reset');
function size(){const bb=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(bb.width||850));canvas.height=Math.max(380,Math.floor(bb.height||530));return[canvas.width,canvas.height]}
function bar(x,y,w,l,val,c,max){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,25);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,Math.abs(val)/max)),25);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText(l+': '+val.toFixed(5),x+8,y+17)}
function draw(step=false){out.textContent=g.value;const gt=+g.value,b1=.9,b2=.999,eta=.01;if(step){t++;m=b1*m+(1-b1)*gt;v=b2*v+(1-b2)*gt*gt}const mh=t?m/(1-Math.pow(b1,t)):0,vh=t?v/(1-Math.pow(b2,t)):0,upd=t?eta*mh/(Math.sqrt(vh)+1e-8):0,[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Adam: momentos adaptativos con corrección de sesgo',24,36);ctx.font='14px monospace';ctx.fillText('θ_{t+1}=θ_t−η m̂_t/(√v̂_t+ε)',24,64);bar(70,128,W*.55,'m̂ dirección',mh,'#818cf8',10);bar(70,172,W*.55,'v̂ escala',vh,'#38bdf8',100);bar(70,216,W*.55,'Δθ Adam',upd,'#f59e0b',.02);ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.70,124,W*.23,128);ctx.strokeStyle='#334155';ctx.strokeRect(W*.70,124,W*.23,128);ctx.fillStyle='#c7d2fe';ctx.font='13px monospace';ctx.fillText('t='+t,W*.72,155);ctx.fillText('β1=0.90',W*.72,185);ctx.fillText('β2=0.999',W*.72,215);readout.innerHTML='<b>m̂='+mh.toFixed(4)+' · v̂='+vh.toFixed(4)+' · Δθ='+upd.toFixed(5)+'</b><br>Adam combina dirección suavizada de Momentum con escala adaptativa tipo RMSProp.'}
stepBtn.onclick=()=>draw(true);reset.onclick=()=>{m=0;v=0;t=0;draw(false)};g.oninput=()=>draw(false);draw(true);return()=>{};
}
