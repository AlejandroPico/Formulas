export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','bio-pk-sim');root.style.setProperty('--equation-color','#ea580c');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>eliminación ke</span><input id="ke" type="range" min="0.05" max="0.60" step="0.05" value="0.25"><output>0.25 h⁻¹</output></label><label><span>dosis</span><input id="dose" type="range" min="100" max="1000" step="50" value="500"><output>500 mg</output></label>';
const ke=controls.querySelector('#ke'),dose=controls.querySelector('#dose'),outs=controls.querySelectorAll('output');const Vd=25;
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const k=+ke.value,D=+dose.value,C0=D/Vd,t12=Math.log(2)/k;outs[0].textContent=k.toFixed(2)+' h⁻¹';outs[1].textContent=D.toFixed(0)+' mg';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Farmacocinética: un compartimento',24,36);ctx.font='14px monospace';ctx.fillText('C(t) = (Dosis/Vd) · e^(−ke t)',24,64);const x=62,y=H-82,w=W*.58,h=H*.56;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x,y-h);ctx.lineTo(x,y);ctx.lineTo(x+w,y);ctx.stroke();ctx.strokeStyle='#ea580c';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const t=i/260*24,C=C0*Math.exp(-k*t),yy=y-C/C0*h;if(i)ctx.lineTo(x+i/260*w,yy);else ctx.moveTo(x,yy)}ctx.stroke();const px=x+Math.min(24,t12)/24*w;ctx.strokeStyle='#fbbf24';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(px,y);ctx.lineTo(px,y-h*.5);ctx.lineTo(x,y-h*.5);ctx.stroke();ctx.setLineDash([]);badge('t1/2',px-12,y-h*.55,'#fde68a');badge('C0='+C0.toFixed(1)+' mg/L',W*.66,140,'#fed7aa');readout.innerHTML='<b>t1/2='+t12.toFixed(2)+' h · C0='+C0.toFixed(2)+' mg/L</b><br>Eliminación de primer orden: cada vida media reduce la concentración a la mitad.'}
ke.oninput=dose.oninput=draw;draw();return()=>{};
}
