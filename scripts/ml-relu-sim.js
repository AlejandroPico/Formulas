export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-relu-sim');root.style.setProperty('--equation-color','#10b981');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>entrada x</span><input id="x" type="range" min="-5" max="5" step="0.2" value="-2"><output>-2.0</output></label><label><span>Leaky α</span><input id="a" type="range" min="0" max="0.3" step="0.01" value="0"><output>0.00</output></label><label><span>umbral desplazado τ</span><input id="tau" type="range" min="-2" max="2" step="0.1" value="0"><output>0.0</output></label>';
const xIn=controls.querySelector('#x'),aIn=controls.querySelector('#a'),tauIn=controls.querySelector('#tau'),outs=controls.querySelectorAll('output');
function f(x,a,t){const z=x-t;return z>=0?z:a*z}function der(x,a,t){return x>=t?1:a}function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const xv=+xIn.value,a=+aIn.value,tau=+tauIn.value,yv=f(xv,a,tau),g=der(xv,a,tau);outs[0].textContent=xv.toFixed(1);outs[1].textContent=a.toFixed(2);outs[2].textContent=tau.toFixed(1);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('ReLU / Leaky ReLU: región activa y zona muerta',24,36);ctx.font='14px monospace';ctx.fillText('f(x)=max(0,x−τ),  Leaky: f(x)=max(α(x−τ),x−τ)',24,64);const cx=W/2,cy=H-100,sc=58;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(55,cy);ctx.lineTo(W-55,cy);ctx.moveTo(cx,85);ctx.lineTo(cx,H-58);ctx.stroke();ctx.fillStyle='rgba(239,68,68,.10)';ctx.fillRect(55,85,cx+tau*sc-55,H-143);ctx.fillStyle='rgba(16,185,129,.10)';ctx.fillRect(cx+tau*sc,85,W-55-(cx+tau*sc),H-143);ctx.strokeStyle='#10b981';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const x=-5+i/260*10,px=cx+x*sc,py=cy-f(x,a,tau)*sc;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();ctx.strokeStyle='#fbbf24';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(cx+tau*sc,85);ctx.lineTo(cx+tau*sc,cy+25);ctx.stroke();ctx.setLineDash([]);badge('τ',cx+tau*sc-6,cy+45,'#fde68a');const px=cx+xv*sc,py=cy-yv*sc;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();badge('f='+yv.toFixed(2),px+10,py-8,'#bbf7d0');badge('muerta',70,105,'#fecaca');badge('activa',W-140,105,'#bbf7d0');readout.innerHTML='<b>x='+xv.toFixed(2)+' · f='+yv.toFixed(2)+' · gradiente='+g.toFixed(2)+'</b><br>'+(xv<tau&&a===0?'Zona muerta: salida cero y gradiente bloqueado.':'La pendiente local mantiene flujo de gradiente; α permite una fuga negativa controlada.')}
xIn.oninput=aIn.oninput=tauIn.oninput=draw;draw();return()=>{};
}
