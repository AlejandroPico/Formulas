export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','electronics-mosfet-sim');root.style.setProperty('--equation-color','#2563eb');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>VGS compuerta</span><input id="vgs" type="range" min="0" max="5" step="0.1" value="3"><output>3.0 V</output></label><label><span>VDS drenaje</span><input id="vds" type="range" min="0" max="5" step="0.1" value="1.5"><output>1.5 V</output></label><label><span>Vth umbral</span><input id="vth" type="range" min="0.8" max="2.5" step="0.1" value="1.5"><output>1.5 V</output></label>';
const vgsI=controls.querySelector('#vgs'),vdsI=controls.querySelector('#vds'),vthI=controls.querySelector('#vth'),outs=controls.querySelectorAll('output');const kn=2.0;
function Id(vgs,vds,vth){const vov=vgs-vth;if(vov<=0)return 0;if(vds<vov)return kn*(2*vov*vds-vds*vds);return kn*vov*vov}
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const VGS=+vgsI.value,VDS=+vdsI.value,Vth=+vthI.value,Vov=Math.max(0,VGS-Vth),id=Id(VGS,VDS,Vth),region=VGS<Vth?'corte':VDS<Vov?'tríodo / lineal':'saturación';outs[0].textContent=VGS.toFixed(1)+' V';outs[1].textContent=VDS.toFixed(1)+' V';outs[2].textContent=Vth.toFixed(1)+' V';const[W,H]=size(),x0=62,y0=H-80,w=W*.60,h=H*.56;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('MOSFET ideal: modelo cuadrático',24,36);ctx.font='14px monospace';ctx.fillText('Id≈kn(2VovVds−Vds²) en tríodo; Id≈knVov² en saturación',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#2563eb';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=280;i++){const vds=i/280*5,val=Id(VGS,vds,Vth),xx=x0+i/280*w,yy=y0-Math.min(1,val/20)*h;if(i)ctx.lineTo(xx,yy);else ctx.moveTo(xx,yy)}ctx.stroke();if(Vov>0){ctx.strokeStyle='#f59e0b';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(x0+Vov/5*w,y0-h);ctx.lineTo(x0+Vov/5*w,y0);ctx.stroke();ctx.setLineDash([]);tag('VDS=VOV',x0+Vov/5*w-30,y0-h-8,'#fde68a')}const px=x0+VDS/5*w,py=y0-Math.min(1,id/20)*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag(id.toFixed(2)+' mA',px+10,py-8,'#bfdbfe');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.68,125,W*.27,175);ctx.strokeStyle='#334155';ctx.strokeRect(W*.68,125,W*.27,175);tag('VOV='+Vov.toFixed(2)+' V',W*.70,162,'#e2e8f0');tag('Id='+id.toFixed(2)+' mA',W*.70,198,'#bfdbfe');tag(region,W*.70,236,region==='corte'?'#fecaca':region.startsWith('tríodo')?'#fde68a':'#bbf7d0');readout.innerHTML='<b>Id='+id.toFixed(3)+' mA · región: '+region+'</b><br>El umbral abre el canal; por debajo hay corte, en tríodo el MOSFET parece resistivo y en saturación actúa como fuente de corriente idealizada.'}
vgsI.oninput=vdsI.oninput=vthI.oninput=draw;draw();return()=>{};
}
