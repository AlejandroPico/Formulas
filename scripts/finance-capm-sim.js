export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','finance-capm-sim');root.style.setProperty('--equation-color','#6366f1');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>beta β</span><input id="b" type="range" min="20" max="250" step="10" value="120"><output>1.20</output></label><label><span>retorno mercado Rm</span><input id="rm" type="range" min="4" max="20" step="1" value="10"><output>10%</output></label><label><span>libre de riesgo Rf</span><input id="rf" type="range" min="1" max="8" step="0.5" value="3.5"><output>3.5%</output></label>';
const bI=controls.querySelector('#b'),rmI=controls.querySelector('#rm'),rfI=controls.querySelector('#rf'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const beta=+bI.value/100,Rm=+rmI.value,Rf=+rfI.value,erp=Rm-Rf,req=Rf+beta*erp,alpha=12-req;outs[0].textContent=beta.toFixed(2);outs[1].textContent=Rm+'%';outs[2].textContent=Rf.toFixed(1)+'%';const[W,H]=size(),x0=72,y0=H-86,w=W*.60,h=H*.54;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('CAPM: rendimiento requerido por riesgo sistemático',24,36);ctx.font='14px monospace';ctx.fillText('E(Ri)=Rf+β[E(Rm)−Rf]',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#6366f1';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x0,y0-Rf/25*h);ctx.lineTo(x0+w,y0-(Rf+2.5*erp)/25*h);ctx.stroke();const px=x0+beta/2.5*w,py=y0-req/25*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag('β '+beta.toFixed(2),px+10,py-8,'#c7d2fe');tag('SML',x0+w-35,y0-(Rf+2.35*erp)/25*h-10,'#c7d2fe');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.29,185);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.29,185);tag('prima mercado='+erp.toFixed(2)+'%',W*.68,162,'#e2e8f0');tag('E(Ri)='+req.toFixed(2)+'%',W*.68,198,'#c7d2fe');tag('α vs 12%='+alpha.toFixed(2)+'%',W*.68,234,alpha>=0?'#bbf7d0':'#fecaca');tag(beta>1?'agresivo':'defensivo',W*.68,270,beta>1?'#fde68a':'#bbf7d0');readout.innerHTML='<b>Rendimiento requerido='+req.toFixed(2)+'% · prima de mercado='+erp.toFixed(2)+'%</b><br>CAPM separa riesgo diversificable y sistemático: solo β debería ser remunerado en equilibrio.'}
bI.oninput=rmI.oninput=rfI.oninput=draw;draw();return()=>{};
}
