export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','finance-duration-sim');root.style.setProperty('--equation-color','#10b981');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>shock tipos Δy</span><input id="dy" type="range" min="-200" max="300" step="25" value="100"><output>+1.00%</output></label><label><span>duración modificada</span><input id="d" type="range" min="100" max="900" step="25" value="435"><output>4.35 años</output></label><label><span>convexidad</span><input id="cx" type="range" min="0" max="80" step="5" value="25"><output>25</output></label>';
const dyI=controls.querySelector('#dy'),dI=controls.querySelector('#d'),cxI=controls.querySelector('#cx'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(420,Math.floor(b.height||540));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const dy=+dyI.value/10000,D=+dI.value/100,C=+cxI.value,lin=-D*dy,conv=lin+.5*C*dy*dy,price=100*(1+conv);outs[0].textContent=(dy>=0?'+':'')+(dy*100).toFixed(2)+'%';outs[1].textContent=D.toFixed(2)+' años';outs[2].textContent=C.toFixed(0);const[W,H]=size(),x0=72,y0=H*.55,w=W*.58,h=H*.46;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Duración de bonos: sensibilidad a tipos de interés',24,36);ctx.font='14px monospace';ctx.fillText('%ΔP ≈ −Dmod·Δy + 0.5·Convexidad·Δy²',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0+h*.55);ctx.moveTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#10b981';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const x=-.03+i/260*.06,y=-D*x+.5*C*x*x,px=x0+(x+.03)/.06*w,py=y0-y*7*h;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();const px=x0+(dy+.03)/.06*w,py=y0-conv*7*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag((conv*100).toFixed(2)+'%',px+10,py-8,conv>=0?'#bbf7d0':'#fecaca');tag('tipos ↑ → precio ↓',x0+16,y0+h*.47,'#cbd5e1');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.29,175);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.29,175);tag('impacto lineal='+(lin*100).toFixed(2)+'%',W*.68,162,lin>=0?'#bbf7d0':'#fecaca');tag('con convexidad='+(conv*100).toFixed(2)+'%',W*.68,198,conv>=0?'#bbf7d0':'#fecaca');tag('precio≈'+price.toFixed(2),W*.68,234,'#e2e8f0');tag(D>6?'muy sensible':'sensibilidad media',W*.68,270,D>6?'#fecaca':'#bbf7d0');readout.innerHTML='<b>ΔP/P≈'+(conv*100).toFixed(3)+'% · precio≈'+price.toFixed(2)+'</b><br>La duración aproxima la pendiente precio-tipo; la convexidad corrige la curvatura cuando el movimiento de tipos es grande.'}
dyI.oninput=dI.oninput=cxI.oninput=draw;draw();return()=>{};
}
