export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','rl-policy-gradient-sim');root.style.setProperty('--equation-color','#6366f1');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>retorno Gt</span><input id="g" type="range" min="-20" max="20" step="2" value="6"><output>+6</output></label><label><span>probabilidad inicial π(a|s)</span><input id="p" type="range" min="5" max="90" step="5" value="35"><output>35%</output></label>';
const gI=controls.querySelector('#g'),pI=controls.querySelector('#p'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,28);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,v)),28);badge(l+': '+(v*100).toFixed(1)+'%',x+8,y+19,'#e2e8f0')}
function draw(){const G=+gI.value,p0=+pI.value/100,delta=.018*G*p0*(1-p0),p1=Math.max(.02,Math.min(.98,p0+delta)),rest=(1-p1)/2;outs[0].textContent=(G>=0?'+':'')+G;outs[1].textContent=(p0*100).toFixed(0)+'%';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Policy Gradient / REINFORCE: reforzar log-probabilidades',24,36);ctx.font='14px monospace';ctx.fillText('∇J(θ)=E[∇logπθ(a|s) · Gt]',24,64);bar(70,130,W*.55,'acción tomada antes',p0,'#818cf8');bar(70,185,W*.55,'acción tomada después',p1,'#6366f1');bar(70,240,W*.55,'cada otra acción',rest,'#64748b');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.66,125,W*.28,155);ctx.strokeStyle='#334155';ctx.strokeRect(W*.66,125,W*.28,155);badge(G>=0?'retorno positivo':'retorno negativo',W*.68,162,G>=0?'#bbf7d0':'#fecaca');badge('Δπ='+(delta*100).toFixed(2)+'%',W*.68,198,'#c7d2fe');badge('exploración estocástica',W*.68,235,'#e2e8f0');readout.innerHTML='<b>π antes='+(p0*100).toFixed(1)+'% · π después='+(p1*100).toFixed(1)+'%</b><br>REINFORCE aumenta la probabilidad de acciones con retorno positivo y la reduce si el retorno observado fue malo.'}
gI.oninput=pI.oninput=draw;draw();return()=>{};
}
