export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','bio-fisher-kpp-sim');root.style.setProperty('--equation-color','#a855f7');
const ctx=canvas.getContext('2d');let u=Array(90).fill(0);for(let i=0;i<7;i++)u[i]=1;let raf=0;
controls.innerHTML='<label><span>difusión D</span><input id="d" type="range" min="0.5" max="6" step="0.5" value="2.5"><output>2.5</output></label><label><span>crecimiento r</span><input id="r" type="range" min="0.02" max="0.16" step="0.01" value="0.08"><output>0.08</output></label>';
const d=controls.querySelector('#d'),r=controls.querySelector('#r'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function step(){const D=+d.value*.16,rr=+r.value,dt=.14,n=[...u];for(let i=1;i<u.length-1;i++){const lap=u[i+1]-2*u[i]+u[i-1],growth=rr*u[i]*(1-u[i]);n[i]=Math.max(0,Math.min(1,u[i]+(D*lap+growth)*dt));}n[0]=1;u=n;}
function draw(){outs[0].textContent=(+d.value).toFixed(1);outs[1].textContent=(+r.value).toFixed(2);for(let k=0;k<2;k++)step();const D=+d.value*.16,rr=+r.value,v=2*Math.sqrt(D*rr),[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Fisher-KPP: onda de invasión biológica',24,36);ctx.font='14px monospace';ctx.fillText('∂u/∂t = D∂²u/∂x² + r u(1−u)',24,64);const x=62,y=H-82,w=W*.62,h=H*.56;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x,y-h);ctx.lineTo(x,y);ctx.lineTo(x+w,y);ctx.stroke();ctx.fillStyle='rgba(168,85,247,.14)';ctx.strokeStyle='#a855f7';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x,y);u.forEach((val,i)=>ctx.lineTo(x+i/(u.length-1)*w,y-val*h));ctx.lineTo(x+w,y);ctx.closePath();ctx.fill();ctx.stroke();ctx.fillStyle='#1e293b';ctx.fillRect(W*.70,135,W*.23,25);ctx.fillStyle='#a855f7';ctx.fillRect(W*.70,135,W*.23*Math.min(1,v/.7),25);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText('v≈2√(Dr): '+v.toFixed(3),W*.70+8,152);readout.innerHTML='<b>Frente viajero activo.</b><br>La difusión dispersa la población y el término logístico rellena el espacio libre hasta saturación.';raf=requestAnimationFrame(draw)}
d.oninput=r.oninput=()=>{u=Array(90).fill(0);for(let i=0;i<7;i++)u[i]=1;};draw();return()=>cancelAnimationFrame(raf);
}
