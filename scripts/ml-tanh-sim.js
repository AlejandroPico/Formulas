export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-tanh-sim');root.style.setProperty('--equation-color','#dc2626');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>entrada x</span><input id="x" type="range" min="-4" max="4" step="0.1" value="1"><output>1.0</output></label><label><span>ganancia a</span><input id="a" type="range" min="0.5" max="3" step="0.1" value="1"><output>1.0</output></label>';
const xIn=controls.querySelector('#x'),aIn=controls.querySelector('#a'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const xv=+xIn.value,a=+aIn.value,yv=Math.tanh(a*xv),grad=a*(1-yv*yv);outs[0].textContent=xv.toFixed(1);outs[1].textContent=a.toFixed(1);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Tangente hiperbólica: activación centrada en cero',24,36);ctx.font='14px monospace';ctx.fillText('tanh(x)=(e^x−e^(−x))/(e^x+e^(−x)),  tanh′=1−tanh²',24,64);const cx=W/2,cy=H/2+20,scx=70,scy=95;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(50,cy);ctx.lineTo(W-50,cy);ctx.moveTo(cx,85);ctx.lineTo(cx,H-55);ctx.stroke();ctx.setLineDash([5,5]);ctx.strokeStyle='#64748b';ctx.beginPath();ctx.moveTo(55,cy-scy);ctx.lineTo(W-55,cy-scy);ctx.moveTo(55,cy+scy);ctx.lineTo(W-55,cy+scy);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#dc2626';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=260;i++){const x=-4+i/260*8,y=Math.tanh(a*x),px=cx+x*scx,py=cy-y*scy;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();const px=cx+xv*scx,py=cy-yv*scy;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();badge('tanh='+yv.toFixed(3),px+10,py-8,'#fecaca');readout.innerHTML='<b>tanh(a·x)='+yv.toFixed(4)+' · gradiente='+grad.toFixed(4)+'</b><br>'+(grad<.08?'Saturación: gradiente casi desvanecido.':'Zona útil: señal centrada en cero y gradiente activo.')}
xIn.oninput=aIn.oninput=draw;draw();return()=>{};
}
