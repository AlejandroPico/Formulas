export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-tanh-sim');root.style.setProperty('--equation-color','#dc2626');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>entrada x</span><input id="x" type="range" min="-4" max="4" step="0.1" value="1"><output>1.0</output></label><label><span>ganancia a</span><input id="a" type="range" min="0.5" max="3" step="0.1" value="1"><output>1.0</output></label><label><span>mostrar derivada</span><input id="show" type="range" min="0" max="1" step="1" value="1"><output>sí</output></label>';
const xIn=controls.querySelector('#x'),aIn=controls.querySelector('#a'),showIn=controls.querySelector('#show'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const xv=+xIn.value,a=+aIn.value,show=+showIn.value>0,yv=Math.tanh(a*xv),grad=a*(1-yv*yv);outs[0].textContent=xv.toFixed(1);outs[1].textContent=a.toFixed(1);outs[2].textContent=show?'sí':'no';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('tanh: señal centrada y saturación simétrica',24,36);ctx.font='14px monospace';ctx.fillText('tanh′(ax)=a(1−tanh²(ax)); salida en [−1,+1]',24,64);const cx=W/2,cy=H/2+20,scx=70,scy=95;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(55,cy);ctx.lineTo(W-55,cy);ctx.moveTo(cx,85);ctx.lineTo(cx,H-55);ctx.stroke();ctx.fillStyle='rgba(239,68,68,.09)';ctx.fillRect(55,85,120,H-140);ctx.fillRect(W-175,85,120,H-140);ctx.setLineDash([5,5]);ctx.strokeStyle='#64748b';ctx.beginPath();ctx.moveTo(55,cy-scy);ctx.lineTo(W-55,cy-scy);ctx.moveTo(55,cy+scy);ctx.lineTo(W-55,cy+scy);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#dc2626';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=280;i++){const x=-4+i/280*8,y=Math.tanh(a*x),px=cx+x*scx,py=cy-y*scy;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();if(show){ctx.strokeStyle='#f59e0b';ctx.lineWidth=2;ctx.beginPath();for(let i=0;i<=280;i++){const x=-4+i/280*8,y=a*(1-Math.tanh(a*x)**2),px=cx+x*scx,py=cy-y/a*70;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();badge('derivada',W*.70,125,'#fde68a')}const px=cx+xv*scx,py=cy-yv*scy;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();badge('tanh='+yv.toFixed(3),px+10,py-8,'#fecaca');readout.innerHTML='<b>tanh(a·x)='+yv.toFixed(4)+' · gradiente='+grad.toFixed(4)+'</b><br>'+(grad<.08?'Saturación: la salida es estable pero el gradiente casi desaparece.':'Zona activa: salida centrada en cero y gradiente útil para backpropagation.')}
xIn.oninput=aIn.oninput=showIn.oninput=draw;draw();return()=>{};
}
