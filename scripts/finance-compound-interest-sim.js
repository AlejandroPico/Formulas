export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','finance-compound-sim');root.style.setProperty('--equation-color','#2563eb');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>tasa anual r</span><input id="r" type="range" min="1" max="25" step="0.5" value="10"><output>10.0%</output></label><label><span>años t</span><input id="t" type="range" min="5" max="40" step="1" value="25"><output>25 años</output></label><label><span>capital P</span><input id="p" type="range" min="1000" max="50000" step="1000" value="10000"><output>$10k</output></label>';
const rI=controls.querySelector('#r'),tI=controls.querySelector('#t'),pI=controls.querySelector('#p'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function money(x){return '$'+x.toLocaleString('es-ES',{maximumFractionDigits:0})}
function draw(){const r=+rI.value/100,t=+tI.value,P=+pI.value,A=P*Math.pow(1+r,t),gain=A-P;outs[0].textContent=(r*100).toFixed(1)+'%';outs[1].textContent=t+' años';outs[2].textContent='$'+(P/1000).toFixed(0)+'k';const[W,H]=size(),x0=70,y0=H-82,w=W*.62,h=H*.56;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Interés compuesto: crecimiento exponencial del capital',24,36);ctx.font='14px monospace';ctx.fillText('A=P(1+r)^t',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#2563eb';ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<=t;i++){const amt=P*Math.pow(1+r,i),px=x0+i/t*w,py=y0-amt/A*h;if(i)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();ctx.fillStyle='#bfdbfe';ctx.beginPath();ctx.arc(x0+w,y0-h,7,0,7);ctx.fill();tag(money(A),x0+w-70,y0-h-12,'#bfdbfe');tag('principal '+money(P),x0+10,y0-P/A*h-8,'#cbd5e1');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.68,125,W*.27,175);ctx.strokeStyle='#334155';ctx.strokeRect(W*.68,125,W*.27,175);tag('A='+money(A),W*.70,162,'#bfdbfe');tag('intereses='+money(gain),W*.70,198,'#bbf7d0');tag('multiplicador x'+(A/P).toFixed(2),W*.70,234,'#e2e8f0');tag(t>25?'tiempo domina':'fase acumulación',W*.70,270,t>25?'#fde68a':'#bbf7d0');readout.innerHTML='<b>A='+money(A)+' · intereses='+money(gain)+'</b><br>El error del prototipo original queda corregido: se usa la variable interesesGenerados y no una referencia inexistente.'}
rI.oninput=tI.oninput=pI.oninput=draw;draw();return()=>{};
}
