export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','bio-nernst-sim');root.style.setProperty('--equation-color','#16a34a');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>[K+] exterior</span><input id="ko" type="range" min="2" max="25" step="1" value="5"><output>5 mM</output></label><label><span>[K+] interior</span><input id="ki" type="range" min="80" max="170" step="5" value="140"><output>140 mM</output></label>';
const ko=controls.querySelector('#ko'),ki=controls.querySelector('#ki'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c,m){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,25);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,v/m)),25);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText(l+': '+v.toFixed(3),x+8,y+17)}
function draw(){outs[0].textContent=ko.value+' mM';outs[1].textContent=ki.value+' mM';const kout=+ko.value,kin=+ki.value,E=61.54*Math.log10(kout/kin),[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Potencial de reversión de Nernst para K+',24,36);ctx.font='14px monospace';ctx.fillText('E_K = 61.54 · log10([K+]out/[K+]in)   a 37 °C',24,64);const x=70,y=110,w=W*.50,h=H*.55;ctx.fillStyle='rgba(22,163,74,.16)';ctx.fillRect(x,y,w,h*.40);ctx.fillStyle='rgba(59,130,246,.14)';ctx.fillRect(x,y+h*.55,w,h*.38);ctx.strokeStyle='#e2e8f0';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(x,y+h*.50);ctx.lineTo(x+w,y+h*.50);ctx.stroke();for(let i=0;i<58;i++){const top=i<kout*1.7,px=x+20+(i*37%(w-40)),py=top?y+22+(i*19%(h*.32)):y+h*.60+(i*23%(h*.28));ctx.fillStyle='#22c55e';ctx.beginPath();ctx.arc(px,py,3,0,7);ctx.fill()}badge('exterior',x+16,y+22,'#bbf7d0');badge('interior',x+16,y+h-20,'#bfdbfe');badge(E.toFixed(1)+' mV',x+w*.56,y+h*.50-10,'#f8fafc');bar(W*.65,135,W*.28,'cociente out/in',kout/kin,'#16a34a',.25);bar(W*.65,175,W*.28,'E + 120 mV',E+120,'#22c55e',180);readout.innerHTML='<b>E_K = '+E.toFixed(2)+' mV</b><br>Si el voltaje de membrana coincide con E_K, el flujo neto de potasio se anula.'}
ko.oninput=ki.oninput=draw;draw();return()=>{};
}
