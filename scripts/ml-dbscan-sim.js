export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-dbscan-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>ε radio</span><input id="eps" type="range" min="18" max="90" step="4" value="34"><output>34 px</output></label><label><span>MinPts</span><input id="min" type="range" min="2" max="6" step="1" value="3"><output>3</output></label>';
const ei=controls.querySelector('#eps'),mini=controls.querySelector('#min'),outs=controls.querySelectorAll('output');
const pts=[[185,205],[210,190],[230,215],[195,235],[260,240],[135,120],[150,145],[170,128],[130,160],[330,120],[360,135]];
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function dist(a,b){return Math.hypot(a[0]-b[0],a[1]-b[1])}
function draw(){const eps=+ei.value,min=+mini.value;outs[0].textContent=eps+' px';outs[1].textContent=min;const[W,H]=size(),scale=W/520,offY=75;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('DBSCAN: núcleos, frontera y ruido por densidad',24,36);ctx.font='14px monospace';ctx.fillText('Nε(p)={q∈D | d(p,q)≤ε}; núcleo si |Nε(p)| ≥ MinPts',24,64);let cores=0,borders=0,noise=0;const scaled=pts.map(p=>[p[0]*scale,p[1]+offY]);scaled.forEach((p,i)=>{const n=scaled.filter(q=>dist(p,q)<=eps).length;let c='#64748b',lab='ruido';if(n>=min){c='#10b981';cores++;lab='núcleo'}else if(scaled.some((q,j)=>j!==i&&dist(p,q)<=eps&&scaled.filter(r=>dist(q,r)<=eps).length>=min)){c='#f59e0b';borders++;lab='frontera'}else noise++;ctx.fillStyle=c;ctx.beginPath();ctx.arc(p[0],p[1],6,0,7);ctx.fill();if(i===0){ctx.strokeStyle='rgba(139,92,246,.55)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(p[0],p[1],eps,0,7);ctx.stroke();badge('p: '+lab,p[0]+10,p[1]-10,c)}});ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.63,125,W*.31,170);ctx.strokeStyle='#334155';ctx.strokeRect(W*.63,125,W*.31,170);badge('núcleo: '+cores,W*.65,165,'#bbf7d0');badge('frontera: '+borders,W*.65,198,'#fde68a');badge('ruido: '+noise,W*.65,231,'#fecaca');badge(eps<26?'ε pequeño: fragmenta':eps>70?'ε grande: fusiona':'ε razonable',W*.65,268,'#ddd6fe');readout.innerHTML='<b>ε='+eps+' · MinPts='+min+' · núcleos='+cores+'</b><br>DBSCAN no necesita fijar K: los racimos aparecen donde la densidad conectada supera el umbral local.'}
ei.oninput=mini.oninput=draw;draw();return()=>{};
}
