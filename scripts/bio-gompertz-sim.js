export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','bio-gompertz-sim');root.style.setProperty('--equation-color','#8b5cf6');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>tasa r</span><input id="r" type="range" min="0.02" max="0.18" step="0.01" value="0.08"><output>0.08</output></label><label><span>N0 inicial %</span><input id="n0" type="range" min="1" max="35" step="1" value="5"><output>5%</output></label>';
const rIn=controls.querySelector('#r'),n0In=controls.querySelector('#n0'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(440,Math.floor(b.width||850));canvas.height=Math.max(380,Math.floor(b.height||530));return[canvas.width,canvas.height]}
function line(x1,y1,x2,y2){ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke()}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function bar(x,y,w,l,v,c,m){ctx.fillStyle='#1e293b';ctx.fillRect(x,y,w,25);ctx.fillStyle=c;ctx.fillRect(x,y,w*Math.max(0,Math.min(1,v/m)),25);ctx.fillStyle='#e2e8f0';ctx.font='12px monospace';ctx.fillText(l+': '+v.toFixed(3),x+8,y+17)}
function draw(){const r=+rIn.value,N0=+n0In.value/100;outs[0].textContent=r.toFixed(2);outs[1].textContent=(N0*100).toFixed(0)+'%';const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 22px sans-serif';ctx.fillText('Modelo de crecimiento de Gompertz',24,36);ctx.font='14px monospace';ctx.fillText('dN/dt = r N ln(K/N)',24,64);const x=62,y=H-82,w=W*.58,h=H*.56;ctx.strokeStyle='#334155';line(x,y-h,x,y);line(x,y,x+w,y);ctx.strokeStyle='#64748b';ctx.setLineDash([5,5]);line(x,y-h,x+w,y-h);ctx.setLineDash([]);ctx.strokeStyle='#8b5cf6';ctx.lineWidth=3;ctx.beginPath();let pk={p:0,N:0,rate:0};for(let i=0;i<=280;i++){const p=i/280,N=Math.exp(Math.log(N0)*Math.exp(-r*p*70)),rate=r*N*Math.log(1/N);if(rate>pk.rate)pk={p,N,rate};const yy=y-N*h;if(i)ctx.lineTo(x+p*w,yy);else ctx.moveTo(x+p*w,yy)}ctx.stroke();ctx.strokeStyle='#fbbf24';ctx.setLineDash([5,5]);line(x+pk.p*w,y-h,x+pk.p*w,y);ctx.setLineDash([]);badge('máxima velocidad',x+pk.p*w-40,y-pk.N*h-10,'#fbbf24');bar(W*.66,135,W*.28,'r',r,'#8b5cf6',.18);bar(W*.66,175,W*.28,'N0',N0,'#f59e0b',.35);readout.innerHTML='<b>Inflexión aproximada en N='+(pk.N*100).toFixed(1)+'% de K</b><br>La curva es sigmoide, pero asimétrica: el frenado relativo aparece pronto y se intensifica hacia K.'}
rIn.oninput=n0In.oninput=draw;draw();return()=>{};
}
