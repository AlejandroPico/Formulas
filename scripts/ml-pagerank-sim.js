export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-pagerank-sim');root.style.setProperty('--equation-color','#2563eb');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>factor de amortiguación d</span><input id="d" type="range" min="0" max="95" step="5" value="85"><output>0.85</output></label><label><span>iteraciones</span><input id="it" type="range" min="1" max="40" step="1" value="20"><output>20</output></label>';
const di=controls.querySelector('#d'),iti=controls.querySelector('#it'),outs=controls.querySelectorAll('output');
const edges={A:['B','C'],B:['C'],C:['A'],D:['C','A']},names=['A','B','C','D'];
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function ranks(d,it){let pr={A:.25,B:.25,C:.25,D:.25};for(let k=0;k<it;k++){let n={A:(1-d)/4,B:(1-d)/4,C:(1-d)/4,D:(1-d)/4};names.forEach(s=>edges[s].forEach(t=>n[t]+=d*pr[s]/edges[s].length));pr=n}return pr}
function arrow(x1,y1,x2,y2){ctx.strokeStyle='#64748b';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();const a=Math.atan2(y2-y1,x2-x1);ctx.fillStyle='#64748b';ctx.beginPath();ctx.moveTo(x2,y2);ctx.lineTo(x2-8*Math.cos(a-.45),y2-8*Math.sin(a-.45));ctx.lineTo(x2-8*Math.cos(a+.45),y2-8*Math.sin(a+.45));ctx.fill()}
function draw(){const d=+di.value/100,it=+iti.value,pr=ranks(d,it);outs[0].textContent=d.toFixed(2);outs[1].textContent=it;const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('PageRank: autoridad como paseo aleatorio estacionario',24,36);ctx.font='14px monospace';ctx.fillText('PR(A)=(1−d)/N + d Σ PR(M)/L(M)',24,64);const pos={A:[W*.23,H*.38],B:[W*.50,H*.26],C:[W*.72,H*.50],D:[W*.32,H*.70]};[['A','B'],['A','C'],['B','C'],['C','A'],['D','C'],['D','A']].forEach(e=>{const p=pos[e[0]],q=pos[e[1]];arrow(p[0],p[1],q[0],q[1])});names.forEach(n=>{const p=pos[n],r=18+pr[n]*80;ctx.fillStyle='rgba(37,99,235,.18)';ctx.beginPath();ctx.arc(p[0],p[1],r,0,7);ctx.fill();ctx.strokeStyle='#2563eb';ctx.lineWidth=2;ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 14px sans-serif';ctx.fillText(n,p[0]-5,p[1]+5);badge((pr[n]*100).toFixed(1)+'%',p[0]-20,p[1]+r+18,'#bfdbfe')});ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.65,105,W*.30,88);ctx.strokeStyle='#334155';ctx.strokeRect(W*.65,105,W*.30,88);badge('teletransporte='+(100*(1-d)).toFixed(0)+'%',W*.67,140,'#bfdbfe');badge('iteraciones='+it,W*.67,172,'#e2e8f0');readout.innerHTML='<b>C domina con PR='+(pr.C*100).toFixed(1)+'%</b><br>El factor d controla cuánto sigue enlaces el navegante aleatorio frente a saltar a cualquier nodo.'}
di.oninput=iti.oninput=draw;draw();return()=>{};
}
