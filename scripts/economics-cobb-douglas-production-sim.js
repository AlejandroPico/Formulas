export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','econ-cobb-sim');root.style.setProperty('--equation-color','#0ea5e9');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>capital K</span><input id="k" type="range" min="10" max="120" step="5" value="40"><output>40</output></label><label><span>trabajo L</span><input id="l" type="range" min="10" max="120" step="5" value="60"><output>60</output></label><label><span>alpha capital</span><input id="a" type="range" min="20" max="80" step="5" value="40"><output>0.40</output></label>';
const kI=controls.querySelector('#k'),lI=controls.querySelector('#l'),aI=controls.querySelector('#a'),outs=controls.querySelectorAll('output');const A=5;
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(540,Math.floor(b.width||920));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function tag(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.92)';ctx.fillRect(x-6,y-16,ctx.measureText(t).width+12,23);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const K=+kI.value,L=+lI.value,alpha=+aI.value/100,beta=1-alpha,Y=A*Math.pow(K,alpha)*Math.pow(L,beta),MPK=alpha*Y/K,MPL=beta*Y/L;outs[0].textContent=K;outs[1].textContent=L;outs[2].textContent=alpha.toFixed(2);const[W,H]=size(),x0=75,y0=H-82,w=W*.60,h=H*.56;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Cobb-Douglas: producción, productividad y escala',24,36);ctx.font='14px monospace';ctx.fillText('Y=A·K^α·L^β ; α+β=1',24,64);ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(x0,y0-h);ctx.lineTo(x0,y0);ctx.lineTo(x0+w,y0);ctx.stroke();ctx.strokeStyle='#0ea5e9';ctx.lineWidth=3;ctx.beginPath();for(let i=5;i<=120;i++){const y=A*Math.pow(i,alpha)*Math.pow(L,beta),px=x0+(i-5)/115*w,py=y0-Math.min(1,y/500)*h;if(i>5)ctx.lineTo(px,py);else ctx.moveTo(px,py)}ctx.stroke();const px=x0+(K-5)/115*w,py=y0-Math.min(1,Y/500)*h;ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,7,0,7);ctx.fill();tag('Y '+Y.toFixed(1),px+10,py-8,'#bae6fd');tag('L fijo: curva producto total vs K',x0+12,y0-h-10,'#bae6fd');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.67,125,W*.28,185);ctx.strokeStyle='#334155';ctx.strokeRect(W*.67,125,W*.28,185);tag('Y='+Y.toFixed(2),W*.69,162,'#bae6fd');tag('MPK='+MPK.toFixed(3),W*.69,198,'#e2e8f0');tag('MPL='+MPL.toFixed(3),W*.69,234,'#e2e8f0');tag('β='+(beta).toFixed(2),W*.69,270,'#cbd5e1');readout.innerHTML='<b>Y='+Y.toFixed(3)+' · MPK='+MPK.toFixed(4)+' · MPL='+MPL.toFixed(4)+'</b><br>Con α+β=1 hay rendimientos constantes a escala, pero cada factor aislado muestra productividad marginal decreciente.'}
kI.oninput=lI.oninput=aI.oninput=draw;draw();return()=>{};
}
