export default function mount({root,canvas,controls,readout}){
root.classList.add('sim-wide','calc-wide','ml-rbf-sim');root.style.setProperty('--equation-color','#ec4899');
const ctx=canvas.getContext('2d');controls.innerHTML='<label><span>γ curvatura</span><input id="g" type="range" min="1" max="12" step="1" value="3"><output>3</output></label><label><span>distancia ||x−x′||</span><input id="d" type="range" min="0" max="6" step="0.1" value="2"><output>2.0</output></label>';
const gi=controls.querySelector('#g'),di=controls.querySelector('#d'),outs=controls.querySelectorAll('output');
function size(){const b=canvas.getBoundingClientRect();canvas.width=Math.max(520,Math.floor(b.width||900));canvas.height=Math.max(430,Math.floor(b.height||560));return[canvas.width,canvas.height]}
function badge(t,x,y,c){ctx.font='12px monospace';ctx.fillStyle='rgba(15,23,42,.9)';ctx.fillRect(x-5,y-15,ctx.measureText(t).width+10,22);ctx.fillStyle=c;ctx.fillText(t,x,y)}
function draw(){const gs=+gi.value,g=gs*.12,d=+di.value,K=Math.exp(-g*d*d);outs[0].textContent=gs;outs[1].textContent=d.toFixed(1);const[W,H]=size();ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='#e2e8f0';ctx.font='bold 23px sans-serif';ctx.fillText('Kernel RBF: similitud radial alrededor de un soporte',24,36);ctx.font='14px monospace';ctx.fillText('K(x,x′)=exp(−γ||x−x′||²)',24,64);const cx=W*.35,cy=H*.52,maxR=145;for(let r=maxR;r>0;r-=5){const sim=Math.exp(-g*(r/32)**2);ctx.fillStyle='rgba(236,72,153,'+(sim*.34)+')';ctx.beginPath();ctx.arc(cx,cy,r,0,7);ctx.fill()}ctx.fillStyle='#ec4899';ctx.beginPath();ctx.arc(cx,cy,7,0,7);ctx.fill();ctx.strokeStyle='#e2e8f0';ctx.stroke();const px=cx+d/6*maxR,py=cy;ctx.strokeStyle='#fbbf24';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,py);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#f8fafc';ctx.beginPath();ctx.arc(px,py,6,0,7);ctx.fill();badge('x′ soporte',cx-28,cy-14,'#fbcfe8');badge('x',px+10,py-8,'#f8fafc');ctx.fillStyle='rgba(15,23,42,.86)';ctx.fillRect(W*.62,125,W*.31,170);ctx.strokeStyle='#334155';ctx.strokeRect(W*.62,125,W*.31,170);badge('distancia='+d.toFixed(2),W*.64,164,'#e2e8f0');badge('γ efectivo='+g.toFixed(2),W*.64,198,'#fbcfe8');badge('K='+K.toFixed(4),W*.64,236,K>.5?'#bbf7d0':'#fecaca');badge(gs>8?'frontera muy local':'frontera suave',W*.64,274,gs>8?'#fecaca':'#bbf7d0');readout.innerHTML='<b>K='+K.toFixed(4)+'</b><br>γ alto contrae la campana y vuelve la frontera más local; γ bajo suaviza la similitud y reduce riesgo de sobreajuste.'}
gi.oninput=di.oninput=draw;draw();return()=>{};
}
