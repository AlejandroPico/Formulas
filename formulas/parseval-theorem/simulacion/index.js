const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','parseval-wide');const cfg=[['A',0.5,2,0.1,1.2]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d'),N=3;function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const A=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const half=W/2,base=H*.74;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(half,18);ctx.lineTo(half,H-78);ctx.stroke();ctx.strokeStyle='#475569';ctx.beginPath();ctx.moveTo(24,base);ctx.lineTo(half-24,base);ctx.moveTo(half+24,base);ctx.lineTo(W-24,base);ctx.stroke();[-1,0,1].forEach(n=>{const px=half*.5+n*44,h=A*A*34;ctx.fillStyle='rgba(245,158,11,.75)';ctx.fillRect(px-8,base-h,16,h)});const cxf=half+half*.5,scaleW=(half-70)/(2*Math.PI);ctx.fillStyle='rgba(139,92,246,.2)';ctx.strokeStyle='#a78bfa';ctx.lineWidth=2.5;ctx.beginPath();ctx.moveTo(cxf-Math.PI*scaleW,base);for(let w=-Math.PI;w<=Math.PI;w+=.03){const X=A*(1+2*Math.cos(w)),p=X*X,px=cxf+w*scaleW,py=base-p*5;ctx.lineTo(px,py)}ctx.lineTo(cxf+Math.PI*scaleW,base);ctx.closePath();ctx.fill();ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Teorema de Parseval',24,36);ctx.font='16px monospace';ctx.fillText('Σ|x[n]|² = (1/2π)∫|X(e^{jω})|²dω',24,64);ctx.font='13px sans-serif';ctx.fillText('Tiempo',32,92);ctx.fillText('Frecuencia',half+32,92);let ef=0,dw=.01;for(let w=-Math.PI;w<=Math.PI;w+=dw){const X=A*(1+2*Math.cos(w));ef+=X*X*dw}ef/=2*Math.PI;const et=A*A*N;readout.innerHTML='<b>E tiempo='+et.toFixed(4)+'</b> · E frecuencia='+ef.toFixed(4)+' · A='+A.toFixed(1)}new ResizeObserver(draw).observe(root);draw();};export default mount;
