const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','polar-wide');const cfg=[['r',20,110,1,80],['θ',0,360,1,45]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+(c[0]==='θ'?'°':'')+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value+(c[0]==='θ'?'°':'');draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const rr=+e[0].value,deg=+e[1].value,rad=deg*Math.PI/180,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.5,scale=Math.min(W,H)*.0042,x=rr*Math.cos(rad),y=rr*Math.sin(rad),px=cx+x,py=cy-y;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1;[30,60,90,110].forEach(r=>{ctx.beginPath();ctx.arc(cx,cy,r,0,Math.PI*2);ctx.stroke()});ctx.beginPath();ctx.moveTo(24,cy);ctx.lineTo(W-24,cy);ctx.moveTo(cx,24);ctx.lineTo(cx,H-92);ctx.stroke();ctx.strokeStyle='#a855f7';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,py);ctx.stroke();ctx.fillStyle='#c084fc';ctx.beginPath();ctx.arc(px,py,6,0,Math.PI*2);ctx.fill();ctx.setLineDash([4,4]);ctx.strokeStyle='#94a3b8';ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(px,cy);ctx.lineTo(cx,cy);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Coordenadas polares',24,36);ctx.font='18px monospace';ctx.fillText('x=r·cos(θ),  y=r·sin(θ)',24,66);readout.innerHTML='<b>x='+x.toFixed(2)+', y='+y.toFixed(2)+'</b> · r='+rr+' · θ='+deg+'°'}new ResizeObserver(draw).observe(root);draw();};export default mount;
