const x=({canvas,controls,readout})=>{let vals=[2,1,11,1,-3,-5],names=['a1','b1','c1','a2','b2','c2'],inp=[];const head=document.createElement('div');head.innerHTML='<b>a1 x + b1 y = c1<br>a2 x + b2 y = c2</b>';controls.appendChild(head);names.forEach((n,i)=>{const e=document.createElement('input');e.type='number';e.value=vals[i];e.title=n;e.style.width='55px';e.style.margin='4px';controls.appendChild(e);inp.push(e)});const g=canvas.getContext('2d');function lineFrom(a,b,c,col){g.strokeStyle=col;g.lineWidth=2;g.beginPath();for(let px=0;px<=canvas.width;px++){let x=(px-canvas.width/2)/42,y=b?((c-a*x)/b):0,py=canvas.height/2-y*42;if(px===0)g.moveTo(px,py);else g.lineTo(px,py)}g.stroke()}function f(){const[a1,b1,c1,a2,b2,c2]=inp.map(e=>+e.value||0),D=a1*b2-b1*a2,Dx=c1*b2-b1*c2,Dy=a1*c2-c1*a2;const w=canvas.width=700,h=330;g.fillStyle='#020617';g.fillRect(0,0,w,h);g.strokeStyle='#334155';g.lineWidth=1;g.beginPath();g.moveTo(0,h/2);g.lineTo(w,h/2);g.moveTo(w/2,0);g.lineTo(w/2,h);g.stroke();lineFrom(a1,b1,c1,'#f59e0b');lineFrom(a2,b2,c2,'#38bdf8');g.fillStyle='#f8fafc';g.font='14px monospace';g.textAlign='left';g.fillText(a1+'x + '+b1+'y = '+c1,20,25);g.fillText(a2+'x + '+b2+'y = '+c2,20,48);if(D!==0){let X=Dx/D,Y=Dy/D,px=w/2+X*42,py=h/2-Y*42;g.fillStyle='#22c55e';g.beginPath();g.arc(px,py,7,0,Math.PI*2);g.fill();g.fillText('('+X.toFixed(2)+', '+Y.toFixed(2)+')',px+10,py-10);readout.innerHTML='<b>D='+D+'</b>; Dx='+Dx+'; Dy='+Dy+'<br>x=Dx/D='+(X).toFixed(3)+' ; y=Dy/D='+(Y).toFixed(3)}else{readout.innerHTML='<b>D=0</b>: no hay solucion unica. El sistema es paralelo, coincidente o degenerado.'}}inp.forEach(e=>e.oninput=f);f();};
export default x;
