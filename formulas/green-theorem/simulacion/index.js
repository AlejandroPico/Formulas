const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','green-wide');const cfg=[['R',1,2.2,0.1,1.6]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const R=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.5,scale=Math.min(W,H)*.17,rad=R*scale;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='rgba(16,185,129,.18)';ctx.beginPath();ctx.arc(cx,cy,rad,0,Math.PI*2);ctx.fill();ctx.strokeStyle='rgba(16,185,129,.45)';ctx.lineWidth=1.5;for(let i=0;i<8;i++){const ang=i*Math.PI/4,rr=rad*.45,x=cx+Math.cos(ang)*rr,y=cy+Math.sin(ang)*rr;ctx.beginPath();ctx.arc(x,y,10,0,1.5*Math.PI);ctx.stroke()}ctx.strokeStyle='#ef4444';ctx.lineWidth=3;ctx.beginPath();ctx.arc(cx,cy,rad,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#ef4444';ctx.beginPath();ctx.moveTo(cx,cy-rad);ctx.lineTo(cx-12,cy-rad-5);ctx.lineTo(cx-10,cy-rad+8);ctx.closePath();ctx.fill();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Teorema de Green',24,36);ctx.font='16px monospace';ctx.fillText('∮(Pdx+Qdy)=∬(∂Q/∂x−∂P/∂y)dA',24,64);const val=2*Math.PI*R*R;readout.innerHTML='<b>rot F=2</b> · área=πR² · circulación=2πR²='+val.toFixed(4)}new ResizeObserver(draw).observe(root);draw();};export default mount;
