# Derivación

Sea `A(x)=∫_a^x f(t)dt`. Si aumentamos ligeramente el límite superior de `x` a `x+h`, el área añadida es aproximadamente `f(x)h`.

Por tanto:

`[A(x+h)-A(x)]/h ≈ f(x)`.

Al tomar el límite cuando `h→0`, queda `A'(x)=f(x)`.
