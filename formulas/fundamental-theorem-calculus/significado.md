# Significado

El teorema fundamental del cálculo conecta dos ideas que parecen distintas: acumular área mediante una integral y medir cambio instantáneo mediante una derivada.

Si `A(x)=∫_a^x f(t)dt`, entonces la derivada de esa acumulación es la propia función: `A'(x)=f(x)`.
