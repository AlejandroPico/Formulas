const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','circumference-wide');const cfg=[['r',15,50,1,35]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value;draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const r=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const ground=H*.68,cx=W*.17,scale=Math.min(1.6,Math.min(H*.28,W*.12)/50),R=r*scale,L=2*Math.PI*r*scale;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(24,ground);ctx.lineTo(W-24,ground);ctx.stroke();ctx.strokeStyle='#94a3b8';ctx.lineWidth=3;ctx.beginPath();ctx.arc(cx,ground-R,R,0,Math.PI*2);ctx.stroke();ctx.beginPath();ctx.moveTo(cx,ground-R);ctx.lineTo(cx,ground);ctx.stroke();ctx.strokeStyle='#f43f5e';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(cx,ground);ctx.lineTo(Math.min(W-32,cx+L),ground);ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Longitud de una circunferencia',24,36);ctx.font='18px monospace';ctx.fillText('L=2πr',24,66);const real=2*Math.PI*r;readout.innerHTML='<b>L='+real.toFixed(3)+'</b> · r='+r+' · d='+(2*r)}new ResizeObserver(draw).observe(root);draw();};export default mount;
