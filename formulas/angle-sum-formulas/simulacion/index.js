const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','angle-sum-wide');const cfg=[['α',0,60,1,30],['β',0,60,1,20]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'°</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value+'°';draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const A=+e[0].value,B=+e[1].value,a=A*Math.PI/180,b=B*Math.PI/180,t=a+b,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W*.2,cy=H*.68,r=Math.min(W,H)*.48;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(24,cy);ctx.lineTo(W-24,cy);ctx.moveTo(cx,24);ctx.lineTo(cx,H-92);ctx.stroke();function ray(ang,col,w){ctx.strokeStyle=col;ctx.lineWidth=w;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(ang)*r,cy-Math.sin(ang)*r);ctx.stroke()}ray(a,'#3b82f6',3);ray(t,'#10b981',4);ctx.strokeStyle='#3b82f6';ctx.beginPath();ctx.arc(cx,cy,42,0,-a,true);ctx.stroke();ctx.strokeStyle='#ef4444';ctx.beginPath();ctx.arc(cx,cy,58,-a,-t,true);ctx.stroke();const x=cx+Math.cos(t)*r,y=cy-Math.sin(t)*r;ctx.setLineDash([4,4]);ctx.strokeStyle='#94a3b8';ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x,cy);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Fórmulas de suma de ángulos',24,36);ctx.font='16px monospace';ctx.fillText('sin(α+β)=sinαcosβ+cosαsinβ',24,64);const sinA=Math.sin(a),cosA=Math.cos(a),sinB=Math.sin(b),cosB=Math.cos(b),id=sinA*cosB+cosA*sinB,direct=Math.sin(t);readout.innerHTML='<b>sin(α+β)='+id.toFixed(4)+'</b> · directo='+direct.toFixed(4)+' · α+β='+(A+B)+'°'}new ResizeObserver(draw).observe(root);draw();};export default mount;
