const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','quotient-wide');const cfg=[['f',10,100,1,50],['g',2,20,1,5]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value;draw()}});const ctx=canvas.getContext('2d'),df=2,dg=1;function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const f=+e[0].value,g=+e[1].value,R=f/g,up=df*g,down=f*dg,num=up-down,den=g*g,der=num/den,[W,H]=size();canvas.width=W;canvas.height=H;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);let cx=W*.5,cy=H*.42;ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Regla del cociente',24,36);ctx.font='18px monospace';ctx.fillText("(f/g)' = (f'g - fg') / g²",24,66);ctx.textAlign='center';ctx.font='bold 42px monospace';ctx.fillStyle='#38bdf8';ctx.fillText('f = '+f,cx,cy-42);ctx.strokeStyle='#e2e8f0';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(cx-110,cy-18);ctx.lineTo(cx+110,cy-18);ctx.stroke();ctx.fillStyle='#fb923c';ctx.fillText('g = '+g,cx,cy+32);ctx.font='bold 22px monospace';ctx.fillStyle='#e2e8f0';ctx.fillText('R = '+R.toFixed(3),cx,cy+78);ctx.textAlign='left';ctx.font='16px monospace';ctx.fillStyle='#38bdf8';ctx.fillText("f'g = "+up.toFixed(2),W*.1,H*.76);ctx.fillStyle='#fb923c';ctx.fillText("fg' = "+down.toFixed(2),W*.38,H*.76);ctx.fillStyle=num>=0?'#10b981':'#ef4444';ctx.fillText('numerador = '+num.toFixed(2),W*.66,H*.76);readout.innerHTML='<b>(f/g)\'='+der.toFixed(4)+'</b> · ['+up.toFixed(1)+' - '+down.toFixed(1)+'] / '+den.toFixed(1)}new ResizeObserver(draw).observe(root);draw();};export default mount;
