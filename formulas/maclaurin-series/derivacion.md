# Derivación

Se busca un polinomio `P(x)` cuyas derivadas en `0` coincidan con las de `f`. Si se impone `P^{(n)}(0)=f^{(n)}(0)`, el coeficiente de `x^n` debe ser `f^{(n)}(0)/n!`.

Así aparece la expansión:

`f(x)=Σ f^{(n)}(0)x^n/n!`.
