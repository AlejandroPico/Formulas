const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','maclaurin-wide');const cfg=[['N',0,6,1,2]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value;draw()}});const ctx=canvas.getContext('2d');function fact(n){let r=1;for(let i=2;i<=n;i++)r*=i;return r}function P(x,N){let s=0;for(let n=0;n<=N;n++)s+=Math.pow(x,n)/fact(n);return s}function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const N=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W*.38,cy=H*.72,sx=W*.13,sy=H*.12;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#1f2937';for(let x=0;x<W;x+=50){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=0;y<H;y+=50){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}ctx.strokeStyle='#475569';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(20,cy);ctx.lineTo(W-20,cy);ctx.moveTo(cx,20);ctx.lineTo(cx,H-34);ctx.stroke();function curve(fn,col,w){ctx.strokeStyle=col;ctx.lineWidth=w;ctx.beginPath();let started=false;for(let px=0;px<W;px++){let x=(px-cx)/sx,y=fn(x),py=cy-y*sy;if(py>-80&&py<H+80){if(!started){ctx.moveTo(px,py);started=true}else ctx.lineTo(px,py)}}ctx.stroke()}curve(x=>Math.exp(x),'#94a3b8',2.5);curve(x=>P(x,N),'#60a5fa',3);ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Serie de Maclaurin',24,36);ctx.font='18px monospace';ctx.fillText('e^x ≈ Σ x^n/n!',24,66);readout.innerHTML='<b>N='+N+'</b> · P_N(1)='+P(1,N).toFixed(5)+' · e='+Math.E.toFixed(5)}new ResizeObserver(draw).observe(root);draw();};export default mount;
