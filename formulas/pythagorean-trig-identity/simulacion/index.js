const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','trig-pythagorean-wide');const cfg=[['θ',0,360,1,45]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'°</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value+'°';draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const deg=+e[0].value,rad=deg*Math.PI/180,sn=Math.sin(rad),cs=Math.cos(rad),[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.48,r=Math.min(W,H)*.25,px=cx+cs*r,py=cy-sn*r;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(24,cy);ctx.lineTo(W-24,cy);ctx.moveTo(cx,24);ctx.lineTo(cx,H-82);ctx.stroke();ctx.strokeStyle='#94a3b8';ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,r,0,Math.PI*2);ctx.stroke();ctx.strokeStyle='#64748b';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,py);ctx.stroke();ctx.strokeStyle='#2563eb';ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,cy);ctx.stroke();ctx.strokeStyle='#ef4444';ctx.beginPath();ctx.moveTo(px,cy);ctx.lineTo(px,py);ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Identidad pitagórica trigonométrica',24,36);ctx.font='18px monospace';ctx.fillText('sin²(θ)+cos²(θ)=1',24,66);ctx.fillStyle='#e2e8f0';ctx.beginPath();ctx.arc(px,py,6,0,Math.PI*2);ctx.fill();readout.innerHTML='<b>sin²+cos²='+(sn*sn+cs*cs).toFixed(4)+'</b> · sin='+sn.toFixed(4)+' · cos='+cs.toFixed(4)}new ResizeObserver(draw).observe(root);draw();};export default mount;
