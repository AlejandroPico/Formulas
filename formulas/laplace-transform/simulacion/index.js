const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','laplace-wide');const cfg=[['s',.4,3,.1,1]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const s=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const ox=W*.12,oy=H*.78,sx=W*.16,sy=H*.48;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#1f2937';for(let x=0;x<W;x+=50){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=0;y<H;y+=50){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}ctx.strokeStyle='#475569';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(ox,20);ctx.lineTo(ox,H-36);ctx.moveTo(24,oy);ctx.lineTo(W-24,oy);ctx.stroke();ctx.fillStyle='rgba(139,92,246,.22)';ctx.beginPath();ctx.moveTo(ox,oy);for(let px=ox;px<W-24;px++){let t=(px-ox)/sx,y=Math.exp(-s*t);ctx.lineTo(px,oy-y*sy)}ctx.lineTo(W-24,oy);ctx.closePath();ctx.fill();ctx.strokeStyle='#a78bfa';ctx.lineWidth=3;ctx.beginPath();for(let px=ox;px<W-24;px++){let t=(px-ox)/sx,y=Math.exp(-s*t);px===ox?ctx.moveTo(px,oy-y*sy):ctx.lineTo(px,oy-y*sy)}ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Transformada de Laplace',24,36);ctx.font='18px monospace';ctx.fillText('L{1}=∫ e^{-st}dt = 1/s',24,66);readout.innerHTML='<b>F(s)='+(1/s).toFixed(4)+'</b> · s='+s.toFixed(2)+' · integrando=e^{-st}'}new ResizeObserver(draw).observe(root);draw();};export default mount;
