# Derivación

La transformada se define ponderando la función temporal por un núcleo exponencial de decaimiento:

`F(s)=∫_0^∞ e^{-st}f(t)dt`.

Para la función escalón `f(t)=1`, el área bajo `e^{-st}` es:

`∫_0^∞ e^{-st}dt=1/s`, si `s>0`.
