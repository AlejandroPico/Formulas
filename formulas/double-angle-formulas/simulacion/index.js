const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','double-angle-wide');const cfg=[['θ',0,90,1,25]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'°</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value+'°';draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const deg=+e[0].value,t=deg*Math.PI/180,d=2*t,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W*.22,cy=H*.68,r=Math.min(W,H)*.46;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(24,cy);ctx.lineTo(W-24,cy);ctx.moveTo(cx,24);ctx.lineTo(cx,H-92);ctx.stroke();function ray(a,col,w){ctx.strokeStyle=col;ctx.lineWidth=w;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(a)*r,cy-Math.sin(a)*r);ctx.stroke()}ray(t,'#3b82f6',3);ray(d,'#f43f5e',4);ctx.strokeStyle='#3b82f6';ctx.beginPath();ctx.arc(cx,cy,44,0,-t,true);ctx.stroke();ctx.strokeStyle='#f43f5e';ctx.beginPath();ctx.arc(cx,cy,62,0,-d,true);ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Fórmulas de ángulo doble',24,36);ctx.font='17px monospace';ctx.fillText('sin(2θ)=2sinθcosθ · cos(2θ)=cos²θ−sin²θ',24,64);const sn=Math.sin(t),cs=Math.cos(t),sin2=2*sn*cs,cos2=cs*cs-sn*sn;readout.innerHTML='<b>2θ='+(2*deg)+'°</b> · sin(2θ)='+sin2.toFixed(4)+' · cos(2θ)='+cos2.toFixed(4)}new ResizeObserver(draw).observe(root);draw();};export default mount;
