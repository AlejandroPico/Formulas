const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','heron-wide');const cfg=[['a',80,180,1,140],['b',80,180,1,150]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value;draw()}});const ctx=canvas.getContext('2d'),c=160;function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const a=+e[0].value,b=+e[1].value,[W,H]=size();canvas.width=W;canvas.height=H;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);const scale=Math.min(W*.55/c,H*.42/180),x0=W*.22,y0=H*.68,x1=x0+c*scale,y1=y0,cosC=(a*a+c*c-b*b)/(2*a*c),sinC=Math.sqrt(Math.max(0,1-cosC*cosC)),x2=x0+a*cosC*scale,y2=y0-a*sinC*scale;ctx.fillStyle='rgba(16,185,129,.22)';ctx.beginPath();ctx.moveTo(x0,y0);ctx.lineTo(x1,y1);ctx.lineTo(x2,y2);ctx.closePath();ctx.fill();ctx.lineWidth=3;ctx.strokeStyle='#64748b';ctx.stroke();ctx.strokeStyle='#ef4444';ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();ctx.strokeStyle='#3b82f6';ctx.beginPath();ctx.moveTo(x2,y2);ctx.lineTo(x0,y0);ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Fórmula de Herón',24,36);ctx.font='18px monospace';ctx.fillText('A=√[s(s−a)(s−b)(s−c)]',24,66);const s=(a+b+c)/2,area=Math.sqrt(Math.max(0,s*(s-a)*(s-b)*(s-c)));readout.innerHTML='<b>A='+area.toFixed(2)+'</b> · s='+s.toFixed(1)+' · a='+a+' · b='+b+' · c='+c}new ResizeObserver(draw).observe(root);draw();};export default mount;
