# Derivación

Sea `I=∫ e^{-x²}dx` en toda la recta. Entonces `I²` puede escribirse como una integral doble sobre el plano:

`I²=∫∫ e^{-(x²+y²)}dxdy`.

Al pasar a coordenadas polares se obtiene:

`I²=∫_0^{2π}∫_0^∞ e^{-r²}rdrdθ=π`.

Por tanto `I=√π`.
