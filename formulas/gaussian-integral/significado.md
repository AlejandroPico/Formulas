# Significado

La integral de Gauss afirma que el área total bajo `e^{-x²}` en toda la recta es `√π`. Es una identidad central porque conecta análisis, geometría polar y probabilidad.

La versión normalizada de esta campana es la base de la distribución normal.
