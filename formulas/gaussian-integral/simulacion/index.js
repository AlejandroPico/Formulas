const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','gauss-wide');const cfg=[['b',.5,3,.1,1.2]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function f(x){return Math.exp(-x*x)}function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const b=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.78,sx=W*.13,sy=H*.48;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#1f2937';for(let x=0;x<W;x+=50){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=0;y<H;y+=50){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}ctx.strokeStyle='#475569';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(20,cy);ctx.lineTo(W-20,cy);ctx.moveTo(cx,20);ctx.lineTo(cx,H-34);ctx.stroke();ctx.fillStyle='rgba(16,185,129,.22)';ctx.beginPath();ctx.moveTo(cx-b*sx,cy);for(let x=-b;x<=b;x+=.03)ctx.lineTo(cx+x*sx,cy-f(x)*sy);ctx.lineTo(cx+b*sx,cy);ctx.closePath();ctx.fill();ctx.strokeStyle='#10b981';ctx.lineWidth=3;ctx.beginPath();for(let px=0;px<W;px++){let x=(px-cx)/sx,y=f(x);px?ctx.lineTo(px,cy-y*sy):ctx.moveTo(px,cy-y*sy)}ctx.stroke();let area=0,steps=260,dx=2*b/steps;for(let i=0;i<steps;i++){let x1=-b+i*dx,x2=x1+dx;area+=.5*(f(x1)+f(x2))*dx}const rootPi=Math.sqrt(Math.PI);ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Integral de Gauss',24,36);ctx.font='18px monospace';ctx.fillText('∫ e^{-x²} dx = √π',24,66);readout.innerHTML='<b>área='+area.toFixed(5)+'</b> · √π='+rootPi.toFixed(5)+' · '+(area/rootPi*100).toFixed(2)+'%'}new ResizeObserver(draw).observe(root);draw();};export default mount;
