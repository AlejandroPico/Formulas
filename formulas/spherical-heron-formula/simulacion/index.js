const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','spherical-heron-wide');const cfg=[['a',0.2,1.2,0.1,0.6]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d'),b=.7,c=.8;function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const a=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.48,R=Math.min(W,H)*.22;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.stroke();ctx.beginPath();ctx.ellipse(cx,cy,R,R*.28,0,0,Math.PI*2);ctx.stroke();ctx.strokeStyle='#a78bfa';ctx.lineWidth=4;ctx.beginPath();ctx.arc(cx-R*.12,cy+R*.08,R*a*.9,-.6,1.25);ctx.stroke();ctx.beginPath();ctx.arc(cx+R*.22,cy-R*.03,R*.55,-2.5,-.25);ctx.stroke();ctx.beginPath();ctx.arc(cx-R*.22,cy-R*.06,R*.62,.2,2.4);ctx.stroke();const s=(a+b+c)/2,prod=Math.tan(s/2)*Math.tan((s-a)/2)*Math.tan((s-b)/2)*Math.tan((s-c)/2),E=4*Math.atan(Math.sqrt(Math.max(0,prod)));ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Fórmula de Herón esférica',24,36);ctx.font='15px monospace';ctx.fillText('tan²(E/4)=Π tan((s−lado)/2)',24,64);readout.innerHTML='<b>E='+E.toFixed(4)+'</b> · área unidad='+E.toFixed(4)+' · s='+s.toFixed(3)+' · a='+a.toFixed(1)}new ResizeObserver(draw).observe(root);draw();};export default mount;
