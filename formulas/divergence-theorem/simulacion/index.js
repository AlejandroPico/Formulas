const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','divergence-wide');const cfg=[['k',0.5,3,0.2,1.5]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const k=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.5,rad=Math.min(W,H)*.2;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#475569';ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(cx,cy,rad,0,Math.PI*2);ctx.stroke();ctx.strokeStyle='#a78bfa';ctx.fillStyle='#a78bfa';ctx.lineWidth=2;for(let i=0;i<16;i++){const a=i*Math.PI*2/16,rx=Math.cos(a),ry=Math.sin(a),x=cx+rx*rad,y=cy+ry*rad,len=k*16;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+rx*len,y+ry*len);ctx.stroke();ctx.beginPath();ctx.arc(x+rx*len,y+ry*len,3,0,Math.PI*2);ctx.fill()}ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Teorema de la divergencia',24,36);ctx.font='16px monospace';ctx.fillText('∯F·n dS = ∭∇·F dV',24,64);const div=2*k,area=Math.PI*Math.pow(rad/40,2),flux=div*area;readout.innerHTML='<b>∇·F='+div.toFixed(1)+'</b> · flujo='+flux.toFixed(4)+' · F=(kx,ky)'}new ResizeObserver(draw).observe(root);draw();};export default mount;
