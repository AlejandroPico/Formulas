# Derivación

Si las raíces son `x1` y `x2`, entonces el polinomio mónico puede escribirse como `(x-x1)(x-x2)`.

Al expandir: `x²-(x1+x2)x+x1x2`. Comparando con `x²+bx+c`, se obtiene `x1+x2=-b` y `x1x2=c`.
