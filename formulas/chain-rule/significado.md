# Significado

La regla de la cadena explica cómo derivar una función compuesta. Si una salida depende de una variable intermedia, el cambio total se obtiene multiplicando la sensibilidad exterior por la sensibilidad interior.

Es la regla natural para procesos encadenados: entrada `x`, transformación interna `u=g(x)` y salida final `f(u)`.
