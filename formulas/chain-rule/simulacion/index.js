const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','chain-wide');const cfg=[['x',.5,3,.1,1.5]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const g2=x=>x*x,gp=x=>2*x,f=u=>Math.sin(u),fp=u=>Math.cos(u),ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function node(x,y,w,h,title,val,sub,c){ctx.fillStyle='rgba(15,23,42,.86)';ctx.strokeStyle=c;ctx.lineWidth=2;ctx.beginPath();ctx.roundRect(x-w/2,y-h/2,w,h,12);ctx.fill();ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 15px sans-serif';ctx.textAlign='center';ctx.fillText(title,x,y-14);ctx.fillStyle=c;ctx.font='bold 22px monospace';ctx.fillText(val,x,y+12);ctx.fillStyle='#94a3b8';ctx.font='12px sans-serif';ctx.fillText(sub,x,y+32)}function draw(){const x=+e[0].value,u=g2(x),y=f(u),du=gp(x),dy=fp(u),tot=du*dy,[W,H]=size();canvas.width=W;canvas.height=H;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);let cy=H*.45,nx=W*.18,ng=W*.5,nf=W*.82;ctx.strokeStyle='#334155';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(nx+70,cy);ctx.lineTo(ng-70,cy);ctx.moveTo(ng+70,cy);ctx.lineTo(nf-70,cy);ctx.stroke();node(nx,cy,120,86,'entrada x',x.toFixed(2),'variable','#64748b');node(ng,cy,140,86,'g(x)=x²',u.toFixed(2),"g'="+du.toFixed(2),'#3b82f6');node(nf,cy,150,86,'f(g)=sin(g)',y.toFixed(2),"f'="+dy.toFixed(3),'#f43f5e');ctx.fillStyle='#e2e8f0';ctx.textAlign='left';ctx.font='bold 24px sans-serif';ctx.fillText('Regla de la cadena',24,36);ctx.font='18px monospace';ctx.fillText("(f∘g)' = f'(g(x)) · g'(x)",24,66);readout.innerHTML='<b>g\'(x)='+du.toFixed(3)+'</b> · f\'(g)='+dy.toFixed(3)+' · total='+tot.toFixed(4)}new ResizeObserver(draw).observe(root);draw();};export default mount;
