# Derivación

Sea `u=g(x)` y `y=f(u)`. Una pequeña variación de `x` produce primero una variación `du`, y esa variación produce después una variación `dy`.

Por tanto:

`dy/dx=(dy/du)(du/dx)`.

En notación funcional:

`(f∘g)'(x)=f'(g(x))g'(x)`.
