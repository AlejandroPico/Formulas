const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','geodesic-wide');const cfg=[['Δθ',20,160,1,90]],inp=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'°</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');inp.push(r);r.oninput=()=>{o.textContent=r.value+'°';draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(280,Math.floor(b.height||430))]}function draw(){const deg=+inp[0].value,rad=deg*Math.PI/180,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W*.52,cy=H*.5,R=Math.min(W,H)*.25;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.stroke();ctx.beginPath();ctx.ellipse(cx,cy,R,R*.32,0,0,Math.PI*2);ctx.stroke();const p1={x:cx-R*Math.sin(rad/2),y:cy+R*Math.cos(rad/2)*.32},p2={x:cx+R*Math.sin(rad/2),y:cy+R*Math.cos(rad/2)*.32};ctx.setLineDash([5,5]);ctx.strokeStyle='#64748b';ctx.lineWidth=1.7;ctx.beginPath();ctx.moveTo(p1.x,p1.y);ctx.lineTo(p2.x,p2.y);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#10b981';ctx.lineWidth=4;ctx.beginPath();let first=true;for(let a=-rad/2;a<=rad/2+.001;a+=.02){const x=cx+R*Math.sin(a),y=cy+R*Math.cos(a)*.32;if(first){ctx.moveTo(x,y);first=false}else ctx.lineTo(x,y)}ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Geodésicas',24,36);ctx.font='15px monospace';ctx.fillText('x″ᵘ+Γᵘᵅᵝx′ᵅx′ᵝ=0',24,64);ctx.fillStyle='#f8fafc';[p1,p2].forEach(p=>{ctx.beginPath();ctx.arc(p.x,p.y,5,0,Math.PI*2);ctx.fill()});const chord=2*R*Math.sin(rad/2),arc=R*rad;readout.innerHTML='<b>arco='+arc.toFixed(1)+'</b> · cuerda='+chord.toFixed(1)+' · Δθ='+deg+'°'}new ResizeObserver(draw).observe(root);draw()};export default mount;
