const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','curve-torsion-wide');const cfg=[['b',0,4,.2,1.8]],inputs=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');inputs.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d'),R=2;function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(280,Math.floor(b.height||430))]}function proj(x,y,z,W,H){const s=Math.min(W,H)*.12,cx=W*.52,cy=H*.56;return{x:cx+(x-y*.48)*s,y:cy-(z-y*.34)*s}}function draw(){const b=+inputs[0].value,[W,H]=size();canvas.width=W;canvas.height=H;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1;for(let i=-3;i<=3;i++){const a=proj(i,-3,-1,W,H),c=proj(i,3,-1,W,H);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(c.x,c.y);ctx.stroke()}ctx.strokeStyle='#94a3b8';ctx.lineWidth=2.6;ctx.beginPath();let first=true;for(let t=-7;t<=7;t+=.05){const p=proj(R*Math.cos(t),R*Math.sin(t),b*t*.26,W,H);if(first){ctx.moveTo(p.x,p.y);first=false}else ctx.lineTo(p.x,p.y)}ctx.stroke();const t=.9,p=proj(R*Math.cos(t),R*Math.sin(t),b*t*.26,W,H),q=proj(R*Math.cos(t+.35),R*Math.sin(t+.35),b*(t+.35)*.26,W,H);ctx.strokeStyle='#e11d48';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke();ctx.fillStyle='#e11d48';ctx.beginPath();ctx.arc(p.x,p.y,6,0,Math.PI*2);ctx.fill();const tau=b/(R*R+b*b||1);ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Torsión de una curva espacial',24,36);ctx.font='16px monospace';ctx.fillText('τ=((r′×r″)·r‴)/||r′×r″||²',24,64);readout.innerHTML='<b>τ='+tau.toFixed(4)+'</b> · hélice R=2 · paso b='+b.toFixed(1)}new ResizeObserver(draw).observe(root);draw()};export default mount;
