const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','riemannian-metric-wide');const cfg=[['g₁₁',.5,2.5,.1,1.5]],inp=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');inp.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(280,Math.floor(b.height||430))]}function draw(){const g=+inp[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.56,step=Math.min(W,H)*.055,dx=2,dy=1.5;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1;for(let i=-8;i<=8;i++){const x=cx+i*step/Math.sqrt(g);ctx.beginPath();ctx.moveTo(x,40);ctx.lineTo(x,H-100);ctx.stroke()}for(let j=-5;j<=5;j++){const y=cy+j*step;ctx.beginPath();ctx.moveTo(30,y);ctx.lineTo(W-30,y);ctx.stroke()}const x2=cx+dx*step/Math.sqrt(g),y2=cy-dy*step;ctx.setLineDash([4,4]);ctx.strokeStyle='#64748b';ctx.strokeRect(cx,cy-dy*step,dx*step/Math.sqrt(g),dy*step);ctx.setLineDash([]);ctx.strokeStyle='#0284c7';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(x2,y2);ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Métrica riemanniana',24,36);ctx.font='17px monospace';ctx.fillText('ds²=gᵢⱼdxⁱdxʲ',24,66);const ds2=g*dx*dx+dy*dy,ds=Math.sqrt(ds2);readout.innerHTML='<b>ds='+ds.toFixed(4)+'</b> · ds²='+ds2.toFixed(2)+' · g₁₁='+g.toFixed(1)}new ResizeObserver(draw).observe(root);draw()};export default mount;
