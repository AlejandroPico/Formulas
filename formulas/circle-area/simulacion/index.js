const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','circle-area-wide');const cfg=[['r',20,110,1,70]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value;draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const r=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.48,scale=Math.min(1,Math.min(W*.32,H*.32)/110),R=r*scale;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='rgba(14,165,233,.22)';ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#38bdf8';ctx.lineWidth=3;ctx.stroke();ctx.strokeStyle='#94a3b8';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+R*Math.cos(.52),cy-R*Math.sin(.52));ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Área del círculo',24,36);ctx.font='18px monospace';ctx.fillText('A=πr²',24,66);const area=Math.PI*r*r;readout.innerHTML='<b>A='+area.toFixed(2)+'</b> · r='+r+' · π≈3.14159'}new ResizeObserver(draw).observe(root);draw();};export default mount;
