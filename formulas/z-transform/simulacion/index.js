const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','z-transform-wide');const cfg=[['a',0.2,1.4,0.1,0.7]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const a=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.48,scale=Math.min(W,H)*.22;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.fillStyle='rgba(14,165,233,.17)';ctx.beginPath();ctx.rect(0,0,W,H);ctx.arc(cx,cy,a*scale,0,Math.PI*2,true);ctx.fill('evenodd');ctx.strokeStyle='#334155';ctx.lineWidth=1;for(let x=0;x<W;x+=50){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=0;y<H;y+=50){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}ctx.strokeStyle='#64748b';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(24,cy);ctx.lineTo(W-24,cy);ctx.moveTo(cx,24);ctx.lineTo(cx,H-80);ctx.stroke();ctx.setLineDash([6,6]);ctx.strokeStyle='#94a3b8';ctx.beginPath();ctx.arc(cx,cy,scale,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#38bdf8';ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,a*scale,0,Math.PI*2);ctx.stroke();const px=cx+a*scale;ctx.strokeStyle='#ef4444';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(px-8,cy-8);ctx.lineTo(px+8,cy+8);ctx.moveTo(px-8,cy+8);ctx.lineTo(px+8,cy-8);ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Transformada Z',24,36);ctx.font='18px monospace';ctx.fillText('X(z)=Σ x[n]z^{-n}   ROC: |z|>|a|',24,66);const stable=a<1;readout.innerHTML='<b>a='+a.toFixed(1)+'</b> · polo z=a · ROC |z|>'+a.toFixed(1)+' · '+(stable?'estable':'inestable')}new ResizeObserver(draw).observe(root);draw();};export default mount;
