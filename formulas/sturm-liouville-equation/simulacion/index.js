const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','sturm-wide');const cfg=[['n',1,5,1,2]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+c[4]+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=r.value}});const ctx=canvas.getContext('2d');let t=0,raf=0,dead=false;function size(){const b=canvas.getBoundingClientRect();return [Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){if(dead)return;const n=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const ox=W*.1,len=W*.8,cy=H*.52,amp=H*.18*Math.cos(t);ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(ox,cy);ctx.lineTo(ox+len,cy);ctx.stroke();ctx.strokeStyle='#34d399';ctx.lineWidth=3;ctx.beginPath();for(let px=0;px<=len;px++){const x=px/len,y=cy-Math.sin(n*Math.PI*x)*amp;px?ctx.lineTo(ox+px,y):ctx.moveTo(ox+px,y)}ctx.stroke();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Sturm-Liouville',24,36);ctx.font='16px monospace';ctx.fillText('-y″=λy,  y(0)=y(1)=0',24,64);ctx.fillStyle='#94a3b8';for(let i=0;i<=n;i++){const x=ox+i*len/n;ctx.beginPath();ctx.arc(x,cy,4,0,Math.PI*2);ctx.fill()}const lambda=n*n*Math.PI*Math.PI;readout.innerHTML='<b>modo n='+n+'</b> · λ=n²π²='+lambda.toFixed(2)+' · nodos internos='+(n-1);t+=.045;raf=requestAnimationFrame(draw)}draw();return()=>{dead=true;cancelAnimationFrame(raf)}};export default mount;
