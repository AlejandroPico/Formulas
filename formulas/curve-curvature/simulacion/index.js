const mount=({root,canvas,controls,readout})=>{root.classList.add('sim-wide','calc-wide','curvature-wide');const cfg=[['x',-2,2,.1,0]],e=[];controls.innerHTML='';cfg.forEach(c=>{const lab=document.createElement('label');lab.innerHTML='<span>'+c[0]+'</span><input type="range" min="'+c[1]+'" max="'+c[2]+'" step="'+c[3]+'" value="'+c[4]+'"><output>'+Number(c[4]).toFixed(1)+'</output>';controls.appendChild(lab);const r=lab.querySelector('input'),o=lab.querySelector('output');e.push(r);r.oninput=()=>{o.textContent=(+r.value).toFixed(1);draw()}});const ctx=canvas.getContext('2d');function f(x){return .3*x*x}function f1(x){return .6*x}function f2(){return .6}function size(){const b=canvas.getBoundingClientRect();return[Math.max(320,Math.floor(b.width||760)),Math.max(260,Math.floor(b.height||420))]}function draw(){const x=+e[0].value,[W,H]=size();canvas.width=W;canvas.height=H;const cx=W/2,cy=H*.62,s=Math.min(W,H)*.16;ctx.fillStyle='#020617';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#334155';ctx.lineWidth=1.4;ctx.beginPath();ctx.moveTo(24,cy);ctx.lineTo(W-24,cy);ctx.moveTo(cx,24);ctx.lineTo(cx,H-92);ctx.stroke();ctx.strokeStyle='#94a3b8';ctx.lineWidth=3;ctx.beginPath();for(let px=0;px<W;px++){const xx=(px-cx)/s,py=cy-f(xx)*s;px?ctx.lineTo(px,py):ctx.moveTo(px,py)}ctx.stroke();const dy=f1(x),ddy=f2(),k=Math.abs(ddy)/Math.pow(1+dy*dy,1.5),R=1/k,n=Math.sqrt(1+dy*dy),nx=-dy/n,ny=1/n,px=cx+x*s,py=cy-f(x)*s,ocx=px+nx*R*s,ocy=py-ny*R*s;ctx.strokeStyle='#38bdf8';ctx.lineWidth=2;ctx.beginPath();ctx.arc(ocx,ocy,R*s,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#ef4444';ctx.beginPath();ctx.arc(px,py,6,0,Math.PI*2);ctx.fill();ctx.fillStyle='#e2e8f0';ctx.font='bold 24px sans-serif';ctx.fillText('Curvatura y círculo osculador',24,36);ctx.font='16px monospace';ctx.fillText('κ=|f″(x)|/[1+(f′(x))²]³ᐟ²,  R=1/κ',24,64);readout.innerHTML='<b>κ='+k.toFixed(4)+'</b> · R='+R.toFixed(2)+' · f′='+dy.toFixed(2)+' · f″='+ddy.toFixed(1)}new ResizeObserver(draw).observe(root);draw();};export default mount;
