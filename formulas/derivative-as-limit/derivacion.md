# Derivación

Se parte de la pendiente media entre `x` y `x+h`:

`m_h=(f(x+h)-f(x))/h`.

Cuando `h` se hace cada vez menor, el segundo punto se aproxima al primero. Si el límite existe, la pendiente media converge a la pendiente instantánea:

`f'(x)=lim_{h→0} m_h`.
